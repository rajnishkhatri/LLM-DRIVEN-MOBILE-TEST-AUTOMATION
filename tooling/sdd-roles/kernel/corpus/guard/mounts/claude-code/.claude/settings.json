{
  "artifact": "hook_mount",
  "events": {
    "PreToolUse": [
      {
        "command": "gate-wrap --descriptors {descriptors} --harness claude-code -- write-guard decide --kernel {kernel} --descriptors {descriptors} --harness claude-code --config {config} --registry {registry} --workspace {workspace} --run-dir {run_dir} --role gd-maker",
        "matcher": "gd-maker"
      },
      {
        "command": "gate-wrap --descriptors {descriptors} --harness claude-code -- write-guard decide --kernel {kernel} --descriptors {descriptors} --harness claude-code --config {config} --registry {registry} --workspace {workspace} --run-dir {run_dir} --role gd-checker",
        "matcher": "gd-checker"
      }
    ]
  },
  "generated_by": "write-guard mount",
  "harness": "claude-code"
}
