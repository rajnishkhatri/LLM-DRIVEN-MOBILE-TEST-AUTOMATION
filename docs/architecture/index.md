# Architecture artifacts — delivery projects — bundle index

OKF bundle. Each entry is a typed Concept. See the convention in [CONVENTIONS.md](../CONVENTIONS.md).

Concepts are nested by artifact home (`worksheets/`, `components/`, `adrs/`, `risk/`)
and then by target, mirroring `.arch/`. Orientation and the layout rules are in
[README.md](README.md). Entries below are grouped by target and ordered by workflow
stage — regenerating this file with `make_bundle.py --recurse-index` restores plain
alphabetical order, so re-group by hand after a regeneration.

**tooling / sdd-roles** (kernel ADRs 0001–0004 Accepted; battle-test 2026-08-09):

- [IR-gate arm/role ownership guide](adrs/tooling/sdd-roles/ir-gate-arm-role-ownership.md) — Proposed guide (not yet an ADR): closes the battle-test finding that o7's IR gate is undeclarable in the current gate vocabulary by splitting kernel gate id `ir-gate` from role-stage work (specifier law, architect ADR, maker implementation, hardener/qa verification), with Arm A/B/C/C-dbg maps and anti-patterns (do not fold into `build`/`tests`; do not prose-only patch role bodies).
- Series ADRs (Accepted): [0001 corpus-as-contract](adrs/tooling/sdd-roles/0001-adopt-corpus-as-contract-with-conformance-gated-reference-validator.md) · [0002 golden-run conformance](adrs/tooling/sdd-roles/0002-golden-run-conformance-for-the-gate-orchestrator.md) · [0003 catalog-as-source](adrs/tooling/sdd-roles/0003-catalog-as-source-projections-as-build-outputs.md) · [0004 kata rig](adrs/tooling/sdd-roles/0004-kata-rig-deterministic-instrument.md).

**mobile-test-automation** (stages 1–4 gates closed 2026-07-26 — **all ten ADRs Accepted, no open blockers**; the spine spec was signed off 2026-07-26 and stage 5 arch-risk is now in progress):

- [Architecture characteristics worksheet — Mobile Test Automation LLM Pipeline](worksheets/mobile-test-automation/characteristics-worksheet.md) — Stage-1 (arch-characteristics) worksheet, revision 3 with the gate closed: seven driving characteristics with objective definitions, measures and caveat classes, the confirmed top-3 (reproducibility, security & privacy, verifiability), six tension pairs, an elimination probe, the three clusters handed to stage 3, and the adjudication of the critical re-review.
- [Logical components — Mobile Test Automation LLM Pipeline](components/mobile-test-automation/logical-components.md) — Stage-2 (arch-components) logical architecture, revision 3 with the gate closed: sixteen components in three clusters after the three directed merges, with roles and requirement assignment, characteristic-driven splits, recomputed coupling and connascence findings, a mermaid component diagram, the ADR carry-forward list, and why pass 1 was withdrawn.
- [Style decision and quantum map — Mobile Test Automation LLM Pipeline](worksheets/mobile-test-automation/style-decision.md) — Stage-3 (arch-style) quantum scoping and style selection, gate closed with one variance: decision-readiness audit with named missing inputs, the four determinations (one quantum, four-concern data topology, two async seams, plain modular monolith after the gate declined the recommended microkernel hybridization), a trade-off matrix over the driving characteristics, the losing candidates and why they lost, the edge/access topology the component stage excluded, the eight-ADR plus seven-fitness-function follow-on list handed to stage 4, and the adjudicated post-gate critical review (five precision corrections accepted, no determination changed).
- [Diagram set — Mobile Test Automation LLM Pipeline (C4-flavored)](components/mobile-test-automation/diagram-set.md) — the three-view C4-flavored diagram set assembled mid-stage-5 from the closed stage 1–4 artifacts and the ten Accepted ADRs: shared key, Context view, Container view with the external-edge SLA register, Component view with the async and provenance registers, the six guideline checks self-audit, the misinterpretation test, and the redraw triggers. A working set — stage-6 (arch-validate) ratification will re-assemble it once risk mitigations land.
- [High-level design pack — Weeks 0–3 Shared Spine](presentations/mobile-test-automation/spine-design-pack.md) — deck-style walkthrough (one idea per page) of the spine design for the chief-architect / delivery-manager / developer buy-in audience, derived entirely from the signed-off baseline (spec post-P1 2026-07-27, plan PLAN-OK 2026-07-28, ADRs 0001–0013) with nothing re-decided: the shared premise and week-3 gate, the three protected characteristics, the ADR one-liners with the honestly-open items, the WP0–WP7 roadmap with the five week-0 asks, the developer rules of the road, and four new D2 presentation views — three lint-passing architecture views (P1 spine context, P2 module map, P3 replay flow) plus a delivery-roadmap flow (P4, same toolchain, outside the architecture linter) — that simplify; the nine-view engineer-grade set stays authoritative. Self-audit alongside. A generated single-file edition ([standalone](presentations/mobile-test-automation/spine-design-pack-standalone.md), images embedded as data URIs, view tables inlined as appendices) exists for sharing outside the repo — rebuild via `build_standalone.py`, never edit by hand.
- [Risk assessment — Mobile Test Automation LLM Pipeline](risk/mobile-test-automation/risk-report.md) — Stage-5 (arch-risk) assessment, in progress: the container-level input diagram with SLA annotations, seven risk criteria against five domain contexts, the five named risk inputs routed from stage 4, the storming pass queue, and the matrix filled one blind-scored pass at a time. Pass P2 (Reproducibility) completed phases 1–2 on 2026-07-26 — four blind lenses, 43 raw rows merged to a 24-entry register (seven 9s, headlined by the unanimous gateway model-version self-report finding), the row agreed at 9/9/9/6/9 after four human arbitrations. Pass P3 (External integrations) completed phases 1–2 on 2026-07-27 — five blind lenses, 76 raw rows merged to a 33-entry register (thirteen 9s, headlined by the Perfecto contractual void, the recurring gateway version-contract finding, and a five-lens automatic 9 on the object repository), the column agreed at 9/9/9/6/6/9/9 (sum 57, the first complete column) after four arbitrations including a prompt-parity override that collapsed two automatic 9s. P3 phase-3 mitigation on the thirteen 9s completed 2026-07-27 (M1–M12): five stakeholder evidence facts recorded (incumbent Perfecto contract, mock data, internal-network scope, internal model providers, the object repository named as Octane), two 9s closed outright by evidence, ten re-scored to 4–6 under accepted mitigations, one held at 9 pending the gateway inquiry; the External integrations column stands at 9/6/9/6/6/9/6 (sum 51), five accepted rules routed to the spine spec, four probes open. P3 phase-3 completed on the rest of the register 2026-07-27: an eleven-entry covered sweep (S1–S11) ruled as an enumerated batch — three held at 6 riding open probes, one accepted as residual, one folded into M8, the rest re-scored to 4 — and nine individual rulings (M13–M21) adding six more spec-routed rules (hash-at-ingest, certify-locally/publish-async, task-zero F1/F2 CI wiring, the corpus-class gate field with a real-input floor, dual-fixture contract tests, queue cap/backoff/dead-letter) plus the week-0 workbook-corpus request; two entries closed by rule at 2, seven re-scored to 4, cells unchanged. All 33 P3 entries adjudicated; the P2 register still awaits mitigation. The spine-spec edit pass completed 2026-07-27: all eleven spec-routed rules plus three riders folded into the spine spec (seven as EARS criteria, the four certification-stage rules plus S7 as a binding carry-forward section CF1–CF5 with a MUST-import obligation on the weeks 3–8 spec); the spec's sign-off gate re-opened and was re-closed by human re-sign-off the same day, making the spec the post-mitigation baseline. P2 phase-3 mitigation completed 2026-07-27: decision D1 recorded (interim embedded/containerized-PostgreSQL posture for dev/CI pending catalog approval, same engine so zero dialect drift, migrations from task zero), an eleven-entry covered sweep (S12–S22) ruled as a batch, and eleven adjudications (M22–M32, M24 grouping the Perfecto-drift facets) — the Reproducibility row drops to 4/4/9/4/9 with the two held 9s riding the object-storage platform probe / ADR 0011 (M22) and the gateway inquiry (M5); new artifacts owed: ADR 0011, carry-forward rules CF6 (K-integrity) and CF7 (fidelity re-derivation via cache replay), two week-0 asks (internal S3-compatible platform storage, PostgreSQL catalog approval), and a small P2 batch of spec-routed field rules (record-actual execution context, CI-runner pinning, prompt version = Git SHA, CLI build identity). The P2 batch spec-edit pass completed 2026-07-27: all six items (the four field rules plus CF6/CF7 joining the carry-forward table) reviewed one at a time and accepted as proposed; the spec's sign-off gate re-opened per its own condition and was re-closed by human re-sign-off the same day, making the spec the post-P2-mitigation baseline — ready for plan + tasks. Pass P1 (Security & privacy) completed phases 1–2 on 2026-07-27 — the first pass run fully post-mitigation: five blind lenses scored residual risk against the E-facts and the whole M/S/CF record, discipline held (no re-raises; all three routed inputs at their adjudicated residuals), 55 raw rows merged to a 21-entry register, and the row agreed at 6/6/6/6/6 (sum 30) after five arbitrations — three ruled below the pass recommendation, headlined by the arbiter overriding the operations lens's automatic 9 on the unnamed test-data vault to 6 (weeks 3–8 machinery). The headline pattern: stage-1 security machinery (vault, redaction-at-capture, manual sampling, retention deletion) that no ADR, spec criterion, or mitigation builds or owns. P1 phase-3 mitigation completed 2026-07-27: evidence fact E6 recorded (an existing secure-SDLC standard this system inherits, ruled to re-score nothing by itself and to enter each ruling per-entry), a five-entry sweep (S23–S27) and ten mechanism-grouped rulings (M33–M43) over the remaining sixteen — the row drops to **4/4/6/4/6, sum 24**, with two cells holding at 6 for reasons outside design's reach (retention enforcement riding M33's mandated-schedule question and ADR 0011; the two unread vendor documents). Unlike P2 and P3, this pass produced design commitments rather than re-scorings: credential isolation from the generated-code execution context (M42 — after finding that ADR 0009's untrusted-input premise and the unsandboxed credential-holding worker are one attack path end to end), the fidelity judge ruled advisory-not-autonomous (M41), and crypto-shredding as the only reconciliation of mandatory immutability with mandatory erasure, with key destruction marking affected verdicts evidence-destroyed (M39). Four honest negatives are on the record: a phase-2 arbitration premise corrected at the gate (M34 — the reference test does authenticate), a top-3 worksheet measure substituted rather than implemented (M36 — manual sampling replaced on statistical-power grounds), a *defective* worksheet measure restated (M37 — "zero anonymous rows" is satisfiable by a shared login), and five entries deliberately held rather than credited. All P1-owed artifacts settled 2026-07-27 at a single **combined gate** (an owner-directed departure from the one-at-a-time review used for P3/P2, recorded in the spec): ADR 0012 and ADR 0013 Accepted, the ADR 0011 crypto-shredding amendment ratified (the ADR itself stays Proposed pending the platform probe), and the P1 spine-spec edit pass applied as a batch — ten spec-routed rules (M34–M40: credential indirection, the three-call-site screening posture with the Acquire UI Evidence site built in the spine, screened-fixture CI markers, quarantine-and-review, novelty sampling + corpus provenance reporting, schema-enforced principals, the hash-chain/grants/supersede package, retention classes with short spine object-lock retention, ephemeral-only dev/CI stores) plus four new carry-forward rows CF8–CF11 (vault-key indirection with the dated blocking flip, the advisory judge, execution isolation, auditor-export inheritance) — with the spec re-signed-off as the post-P1-mitigation baseline; ADR 0009 already carries two amendments from the pass (boundary scoping with the flip counter at 2 of 3; the red-team corpus given an owner, a forcing function, and provenance reporting). Pass P4 (Verifiability) completed phases 1–2 on 2026-07-27 — the second fully post-mitigation pass: five blind lenses, 44 raw rows merged to an 18-entry register (10 shared clusters + 8 single-lens finds), the row agreed at **6/9/9/6/9 (sum 39, the highest-standing row)** after five arbitrations, all ruled at the pass recommendation. The headline: three mitigation passes hardened the certification *verdict* but nothing binds the regression suite's *contents* to one — two lenses independently found suite-admission unenforced at 3×3 (P4-1, arbitrated into V&C; the fix is one CI-blocking membership fitness function), and the calibration record anchoring the subjective gate is softer than everything it anchors (no held-out discipline, labels self-graded, TPR/TNR not re-derivable). The E&P automatic 9 extends M22's held state to this row via CF1's custody precondition; the External 9 unanimously confirms the P3 adjudication riding M5. P4 phase 3 and pass P5 remain queued.

Stage-4 (arch-decide) ADRs, filed under `adrs/application/mobile-test-automation/` — 0001–0010 Accepted 2026-07-26; 0011 filed Proposed 2026-07-27 from stage-5 risk mitigation, acceptance blocked on the week-0 platform probe. **F1–F7 are the cross-cutting fitness functions; each ADR's Compliance section carries further governance mechanisms, and arch-validate must inventory all of them rather than the seven alone.**

- [ADR 0001 — Route every model call through a single Invoke Models seam](adrs/application/mobile-test-automation/0001-route-every-model-call-through-invoke-models.md) — the mandatory compensating control for Evolvability's displacement: one Spring-interface seam with two named implementations (Phase 1 Copilot chat/agent, Phase 2 direct Orchestrator AI calls) and a config-only cutover, the IR spine as the only crossing vocabulary, and fitness functions F1/F2 plus a golden-set contract-parity check as its Compliance section.
- [ADR 0002 — Include model and provider version in the response-cache key](adrs/application/mobile-test-automation/0002-include-model-and-provider-version-in-cache-key.md) — closes the stage-2 defect where hash(input + prompt_version) would silently serve cached output from a different model after a gateway model change.
- [ADR 0003 — Coordinate conversion with central orchestration](adrs/application/mobile-test-automation/0003-coordinate-conversion-with-central-orchestration.md) — orchestration over choreography for the conversion state machine; CE = 11 accepted because CA = 0, with the no-policy-in-the-coordinator mitigation as a compliance rule.
- [ADR 0004 — Obtain fidelity grades synchronously inside certification](adrs/application/mobile-test-automation/0004-obtain-fidelity-grades-synchronously-inside-certification.md) — accepts the entanglement created by the stage-2 judge merge at one quantum; grades are recorded evidence with pinned calibration versions, never re-derived on retry; cluster-B extraction is the expiry condition.
- [ADR 0005 — Build one deployable, a modular monolith partitioned by cluster](adrs/application/mobile-test-automation/0005-adopt-plain-modular-monolith-partitioned-by-cluster.md) — the style determination as decided at the stage-3 gate, with the declined microkernel as the recorded losing alternative and the blueprint's five pipeline-stage modules demoted to packages.
- [ADR 0006 — Partition data by lifecycle in a single primary store](adrs/application/mobile-test-automation/0006-partition-data-by-lifecycle-in-single-primary-store.md) — lifecycle-partitioned schemas, blob storage for classified artifacts, Git as the asset store. Accepted once both blockers closed: the security half via ADR 0010 and the residency input as **on-premises**, which permits co-location so the cluster-C extraction fallback stays unpulled. Records two adjacent residency exposures it does not own — the Perfecto device cloud and model-provider egress.
- [ADR 0007 — Queue device replay and human decisions; write provenance synchronously via a transactional outbox](adrs/application/mobile-test-automation/0007-queue-two-seams-outbox-provenance-writes.md) — exactly two async seams, local transactions with an outbox on the producer and idempotent consumers, never a distributed transaction across the queue.
- [ADR 0008 — Serve reviewers a web queue, engineers CLIs, and auditors an export — no BFF](adrs/application/mobile-test-automation/0008-review-ui-clis-auditor-export-no-bff.md) — the edge topology from stage-3 §6, closing the unowned-capability finding by assigning the auditor export to Preserve Provenance.
- [ADR 0009 — Screen untrusted content in a shared library invoked at three trust boundaries](adrs/application/mobile-test-automation/0009-screen-untrusted-content-in-a-shared-library.md) — records the stage-2 screening demotion that the stage-3 handoff enforced through F3 without ever recording as a decision; owns F3 across all three trust boundaries, splits it into its static and runtime halves, and states the forfeited structural boundary plainly.
- [ADR 0010 — Run security review as a parallel track that can revise but not block](adrs/application/mobile-test-automation/0010-security-review-as-a-parallel-non-blocking-track.md) — target-scoped override of the workspace approval criteria, resolving the inconsistent handling of the security trigger across 0006, 0008 and 0009. Records the security-owner role as dual-hatted for Phase 1 and states plainly that the resulting control is neither independent nor blocking — a named stage-5 risk input, not a process improvement.
- [ADR 0011 — Access evidence object storage through an S3-compatible port; bind production by the platform probe](adrs/application/mobile-test-automation/0011-evidence-object-storage-behind-s3-port.md) — the object-storage decision spun out of stage-5 risk mitigation M22 (the P2 register's automatic 9), Proposed with acceptance blocked on the week-0 platform probe and a recorded default (self-operated MinIO with object-lock immutability) so silence has a defined outcome before the first real evidence artifact lands; all evidence access through a thin S3 port with dev/CI on containerized MinIO, and M23's backup/restore coherence rules (restore-ordering invariant, post-restore custody reconcile, immutability as precondition) folded in as their own section. **Amended 2026-07-27 (P1 mitigation M39)** with an erasure-under-immutability section: envelope encryption under per-conversion keys, crypto-shredding as the erasure mechanism, key destruction marking affected verdicts `EVIDENCE_DESTROYED` because erasure destroys the reconstructibility CF1 and CF7 depend on, a break-glass path for lineage-resident PII, and the timing rule that spine retention stays short while encryption lands before the first audit-retained artifact.
- [ADR 0012 — Make lineage tamper-evident with a per-conversion hash chain anchored in immutable object storage](adrs/application/mobile-test-automation/0012-tamper-evident-lineage-hash-chain-anchored-in-immutable-storage.md) — the lineage-integrity decision spun out of stage-5 risk mitigation M38 (P1 entry P1-5), on the finding that "append-only" in PostgreSQL is a grant plus a convention and nothing made tampering evident. Anchoring is the load-bearing half: chain heads written under object lock mean a database-side rewrite cannot also rewrite the evidence of the rewrite, with no key material to manage. Chain scope is the conversion (no contention the domain does not already have), the digest joins the existing write transaction (ADR 0007's invariant preserved), corrections become compensating appends, and grants restrict the application role to INSERT/SELECT under an automated assertion. States plainly that it detects rather than prevents, and makes ADR 0011's object lock a hard requirement rather than a preference. **Accepted 2026-07-27** at the combined gate (spec post-P1-mitigation re-sign-off + both new ADRs + the ADR 0011 amendment, one approve-all decision); the security review runs as a parallel track per ADR 0010 as amended.
- [ADR 0013 — Isolate generated-code execution from credentials; commit the process shape now, the sandbox technology later](adrs/application/mobile-test-automation/0013-isolate-generated-code-execution-from-credentials.md) — the execution-isolation decision spun out of stage-5 risk mitigation M42 (P1 entry P1-15), which followed ADR 0009's untrusted-input premise one step further than any prior artifact and found the far end: injected source text steers generation, and the generated code executes on the worker holding the Perfecto, gateway, and test-account credentials. Records why E3 cannot dispose of it — the internal-network fact cannot dismiss the threat model an accepted ADR is built on. Chooses to remove the prize rather than build the cage (impact reductions survive new escape techniques; likelihood reductions do not): no long-lived credential in the execution context, no gateway credential at all, a single-run device session token, separate-process execution as a shape commitment with the sandbox technology at the last responsible moment, and static capability rules declared a supplement rather than the control. **Accepted 2026-07-27** at the same combined gate, with the short-lived-session-credential assumption recorded alongside its fallback rather than verified first; the security review runs as a parallel track per ADR 0010 as amended.
