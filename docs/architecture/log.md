---
type: log
title: 'Architecture artifacts — delivery projects — bundle log'
---

# Architecture artifacts — delivery projects — bundle log

Chronological history, newest first (ISO-8601).

- 2026-08-11 — **OKF type backfill** on 17 Concepts that lacked frontmatter: ADR 0015; tooling/sdd-roles ADRs 0001–0006; explainer `o7-pipeline-map-analogy.md`; presentation P1–P3 view/detail companions; worksheet quantum-map view/detail + SELF-AUDIT. Types match siblings (`architecture`, explainer as `overview`). Catalog left hand-grouped (not regenerated). Lint: 0 failures; remaining WARNs are forward links to `battle-test-report.md`.
- 2026-08-09 — **Live catalog: `ir-gate` appended to role-registry stage exits** (`tooling/sdd-roles/kernel/catalog/role-registry.json`) per the ownership guide defaults — **coder, qa, maker3, checker3, solo** declare `ir-gate`; specifier/architect/cleaner/hardener unchanged. Arms A/B/C/C-dbg role lists unchanged. KernelConfig tool binding still deferred (registry can name the id before the checker binary exists). Guide updated to mark the table applied.
- 2026-08-09 — **Filed proposed guide: IR-gate arm/role ownership** (`adrs/tooling/sdd-roles/ir-gate-arm-role-ownership.md`). Captures the battle-test highest-value finding (o7 IR gate is a first-class "MUST run first" gate no role can declare) as an ownership split: new kernel gate id `ir-gate` (authority) vs specifier law / architect ADR / maker checker code / hardener+qa verify (stage work). Maps Arms A/B/C/C-dbg and a draft who-declares-`ir-gate` table; rejects folding into `build`/`tests` or prose-only role-body patches. Not an Accepted ADR — precursor to a likely ADR 0005. Sourced from `docs/skills/sdd-roles/evals/battle-test-report.md`.
- 2026-08-07 — **SDD Roles Write Guard BUILD ITEM 3 IMPLEMENTED (the D7 floor) — gate GREEN: extended `contract-lint selftest` passes all 16 sections byte-identically across double runs (zero DEFERRED, 12 flipped item-2 cases held, 23 guard decision cases), clean-copy offline acceptance exits 0 at `schema_version` 1.2.0 / validator 0.3.0.** Third same-day sdd pass: spec `docs/sdd/specs/sdd-roles-guard.spec.md` with clarify **C1–C4 owner-locked** (C1 golden-decision conformance, **no new CHK ids** — ADR 0002 extended to guard behavior; C2 hook facts as **additive descriptor-row `hooks` fields at 1.2.0** via `regen_corpus.py restamp --to`; C3 **stateless guard** — stdout/stderr only, no spoofable channel; C4 **verbatim 12-category TDFlow rubric** adopted at `kernel/docs/tamper-rubric.md` with the memo's "11-category" citation corrected forward) → combined SPEC-OK + PLAN-OK, **F1–F4 ratified** (F1 fail-closed exit split: argv misuse → 1, every evaluation failure → 2 `block FAIL_CLOSED` because some hook protocols treat exit 1 as non-blocking; F2 closed `hooks` object shape; F3 decision-line protocol + 7 reason codes frozen as contract; F4 versions) → all 28 tasks (G01–G28). What now exists: the **`write-guard` CLI** (4th console script) — `decide` runs the normative 8-rule procedure (repo-scope escape incl. symlink-ancestor resolution before lexical collapse, `.git/` block, writer-only run-dir/ledger, hard-protected set incl. the **anti-unhook rule** on `harness_enablement`, diff-aware tests rule resolved from disk existence, write scopes) and `mount` renders descriptor `hooks` data into per-harness hook-config artifacts (one rule per registry role, `gate-wrap`-wrapped — the item-1 "exercised under hook mounting" clause executed; only `{role}` bound so artifacts stay byte-portable); **`scopes.py`** extracted as the single rule source consumed by BOTH the retro CHK-SCOPE and the live guard (WP0 refactor certified byte-identical to the item-2 baseline before any feature landed); **guard corpus**: 23 decision cases (fail-closed trio, multi-path first-block ordering, pointer-extraction, runtime-built symlink escape — never committed) + 4 mount goldens + the **`orchestrator-hooked`/`orchestrator-unhooked` golden pair**: hooked proves the live chain (mount → stub consults hook → gate-wrap translates → guard blocks → tamper never lands, **no ledger trace** — stateless S6) with `contract-lint` green; unhooked is the committed **negative control** — same tamper, no mount, write lands, run fails exit 2 validating red on **exactly CHK-SCOPE**, with **zero `gate-runner`/`gate-wrap` line changes** (byte-verified against the item-2 snapshot). Tamper-verified before trusting green: doctored mount byte → `guard-mount` red; doctored decision expectation → `guard-decisions` red; flipped digest hex char in the hooked golden ledger → `orchestrator-goldens` red; each restored reproduces certification bytes. Honest notes: (1) **CHK-NEUTRAL caught the guard's own docstring** ("Claude-semantics" token) on first golden regen — the per-invocation self-integrity scan covered the new module automatically; (2) the schema **closedness walker rejected the dictionary-shaped operation map** — converted to `{from,to}` row-array form (the house "no open-key objects" rule); (3) **CHK-ARM flagged the lone-maker unhooked arm** — resolved by the declared `ablation` marker; (4) the first tamper-3 attempt was a **no-op sed** (pattern lived in the handoff artifact, not the ledger) — redone against a real byte; tamper verification must verify the tamper landed. Conformance doc gains **port admission #8** (guard ports: decision corpus + mount goldens + hooked/unhooked pair from the same rule source). D7 strong tier remains declined (§7a). Next per memo §7: **item 4 — the table-driven emitter** (canonical catalog → Claude plugin / Cursor loose-file / Copilot dual-target projections, consuming the same descriptor `hooks` data).
- 2026-08-07 — **SDD Role Orchestrator BUILD ITEM 2 IMPLEMENTED — gate GREEN, the DEFERRED era is over: extended `contract-lint selftest` passes all 14 sections (byte-identical across runs, zero DEFERRED entries anywhere) and the clean-copy offline acceptance run exits 0.** One same-day sdd pass end-to-end: spec drafted + clarify **C1–C4 owner-locked** (C1 additive fields at `schema_version` **1.1.0**; C2 **two-tree model** + fixture regeneration; C3 **stub-harness golden-run gate**; C4 serialization now) → SPEC-OK + PLAN-OK (**F1–F4 ratified as drafted**) at one combined gate → **ADR 0002 Accepted** (*Golden-run conformance for the gate orchestrator* — extends 0001's corpus-as-contract to behavior) → all 32 tasks (R01–R32). What now exists at `tooling/sdd-roles/` (~312 files): the **seven run-directory checks + two dual item-2 halves LIVE** in `contract-lint` (`checks/run_directory.py` + `ledger_model.py`, the single formula source both checks and runner consume; `checks/deferred.py` deleted; CHK-DEFER **inverted** — an implemented check reporting DEFERRED is now the failure); the **`gate-runner` CLI** (third console script): between-run conveyor driving headless one-shot role invocations from descriptor data, sole ledger writer, per-gate bounded repair + cross-stage rework edges with carried counters, spec-kit serialization at handoff boundaries, cold-start `resume` (byte-equal completion proven) and `--parent` continuation (child counter continues parent's), exit taxonomy {0,1,2} with the "honestly-failed run still validates green" rule; the **1.1.0 restamp** executed by committed `regen_corpus.py` (validity-preserving: valid relations re-synced, deliberate fixture defects kept broken, `expect.json` never written — idempotent, second run writes 0 files); **corpus deltas**: all 12 committed item-2 cases flipped per `expected_when_implemented` (incl. the `added-under-tests` pass-when-implemented case green under the full live suite), new cases `CHK-GATE-BIND/tree-mismatch`, `CHK-GENESIS/counter-not-carried`, `CHK-REWORK/over-cap-across-parent`, `CHK-DEFER/deferred-after-implementation` (replacing `report-claims-green`), cold-start regenerated to the C2 shape (reports + `input_tree_digest` at the `gate_run` entry), and **four golden run sets** (green / rework / resume / parent) the pinned runner reproduces byte-identically ×2, each validating green under all 26 checks. **Normative amendment recorded forward (C2):** the item-1 CHK table's GATE-BIND tree arm was unsatisfiable as written — ledger `tree_digest` is the run-directory artifacts-map digest (fixture-verified byte-for-byte), `input_tree_digest` is the workspace tree, and binding now requires gate id + report digest in the entry's artifacts + writer + input-tree equality; `ledger-chain.md` carries the two-tree section. **Tamper-verified before declaring green:** doctored golden byte → `orchestrator-goldens` red; silenced corpus defect → `invalid-corpus` red; doctored VERSION → drift red. **Three defects were caught by the gate itself during bring-up** (restamp missed `writes[].sha256_after` re-sync; runner double-recorded rework edges — CHK-REWORK monotonicity flagged it; SpecKitMapping didn't travel with the run — genesis now copies config+registry+mapping for S8 self-containment): the gate finding real bugs in its own builder is the mechanism working. Next per memo §7: **item 3 — D7 floor** (live test-folder write-block, repo-scoped FS; the `writes[]` recording and protected-set data it enforces are now real), then item 4 emitter, item 5 role bodies, item 6 kata.

- 2026-08-07 — **SDD Role Kernel BUILD ITEM 1 IMPLEMENTED — gate GREEN: `contract-lint selftest` passes all 12 sections (byte-identical across runs) and the clean-copy offline acceptance run exits 0. The kernel now exists at `tooling/sdd-roles/` (~200 files): 8 closed JSON Schemas, the 26-family golden corpus, the Python reference `contract-lint` + `gate-wrap`, and the four kernel docs.** All 44 tasks (K01–K44) landed in one sdd-implement session, corpus-first as planned. What the gate proves, per the spec's acceptance bar: registry↔corpus **bijection** (26 CHK ids ↔ 26 invalid families — no second list to drift); every corpus case behaves **exactly per its expect.json annotation** (valid → exit 0; v1-fail → exit 2 with the named check id at the annotated JSON pointer; the 12 item-2 cases → DEFERRED, never green — CHK-DEFER); **all three exit codes** exercised ({0,1,2}; errors/ family covers usage=1); the **gate-wrap mapping corpus** covers every exit-code-map row × 3 harness rows incl. gate-tool exits 2/3 (any-non-zero-deny harness row translates 2→deny/1 purely from descriptor data — F1 honored); **determinism** (double-sweep byte-identity + timestamp-free canonical reports); **offline** (socket-denying guard + canary); **CHK-NEUTRAL** live token scan green over schemas + validator source (token list lives in `kernel/neutral_tokens.json`, outside the scan scope, so the scanner itself stays token-free); **CHK-SELF** temp-copy verdict stability over every case (verdicts resolved from `artifact_type` content, never paths). **Adversarial tamper checks run before declaring green:** doctored VERSION → drift red; harness token smuggled into a schema → CHK-NEUTRAL red (and propagates into every validate report); a silenced invalid case → invalid-corpus red — the gate flips, it is not decorative. **One implementation defect was caught by the selftest itself during bring-up** (validation reports keyed by case basename collided across families — `missing-row` exists under both CHK-MAP and CHK-HARNESS-ROWS — silently cross-wiring two assertions; fixed to corpus-relative labels): the anti-gaming machinery caught a real bug in its own harness on first contact. **Implementation notes recorded on the tasks header:** interpreter is Python 3.12.2 via `--system-site-packages` (this machine's 3.11 lacks ensurepip/jsonschema; `requires-python >=3.11` holds); the five self-integrity checks additionally run per-invocation in every `validate` report (every report names all 26 check ids — stronger than the drafted selftest-only scope); named-check routing is documented in `kernel/docs/provenance.md` (schema deliberately leaves fence URI/timestamp, tool version/digest, thresholds, decision floors, and provenance presence check-enforced so failures surface under their NAMED CHK ids, not generic CHK-SCHEMA); the spec's `artifacts{path: sha256}` map notation is realized as a closed array of `{path, sha256}` objects (closedness under C2 wins; noted in `kernel/docs/ledger-chain.md`). Statuses flipped: spec → IMPLEMENTED line, plan §7 + tasks header carry the build record. **Next in the memo §7 build order: item 2 (between-run gate orchestrator), which flips the seven DEFERRED run-directory checks to live and first exercises the ledger for real.**

- 2026-08-07 — **SDD Role Kernel: PLAN-OK GATE CLOSED (F1–F4 ratified as drafted, owner) — ADR 0001 `tooling/sdd-roles` flipped Accepted; Stage-3 tasks K01–K44 drafted; Stage-4 analyze run, no CRITICAL findings. Next: sdd-implement, K01 first.** The plan gate closed in one owner decision ratifying all four flagged readings: F1 exit-code scope (contract-lint strictly {0,1,2}; gate-wrap's mapped exits are InvocationDescriptor data, own usage errors = 1), F2 CHK-PROTECT as a `protected` object with 7 required named keys, F3 harness `declares[]` inside the descriptor set document (neutral `minItems: 3`), F4 `schema_version` 1.0.0. `docs/sdd/plans/sdd-roles-kernel.tasks.md` decomposes the plan into **44 atomic tasks (K01–K44) across WP0–WP6** with file-level specificity, dependency + [P] parallelization markers, and per-task executable pass/fail; the Stage-3 checklist judged **all 30 EARS bullets measurable, none flagged back to the spec** — with two resolved classes recorded honestly: the seven pure item-2 criteria are measurable in v1 only as committed-corpus-family + DEFERRED-reporting (C5's normative Phase column), and HP4's port rule is measurable as documented admission procedure + corpus completeness. The §1 matrix maps criteria↔tasks 1:1 in both directions; all 26 CHK ids carry an implementing (or DEFERRED-stub) task AND a corpus-family task. Build order is corpus-first (schemas + valid corpus before check logic), with K03's selftest ratchet red from day 0 and green exactly at completion. **Stage-4 analyze findings:** one cross-artifact defect found and fixed during decomposition — the plan (and the prior log entry) said **six** item-2 DEFERRED stubs where the spec's CHK table has **seven** pure item-2 rows (CHK-CHAIN/TREE/WRITER/GENESIS/GATE-BIND/SCOPE/REWORK) + 2 dual-phase rows; plan corrected in four places under its §4 adjustable-values clause, correction recorded here rather than rewriting the earlier entry (corrections annotate forward). Two §4-class values added at tasks time: `artifact_type` required const per schema (loader resolves schemas from content, never paths — load-bearing for CHK-SELF) and `expected_when_implemented` on item-2 expect.json sidecars (item 2 inherits executable annotations). **Grounding probed on-machine:** python3.11 = 3.11.13 ✓, jsonschema 4.23.0 inside the `>=4.21,<5` pin ✓ (offline clean-clone run viable via local wheel), `tooling/sdd-roles/` not yet present (K01 creates it, no collision), binding seam + ADR 0001 in place ✓; no undeclared dependency in any task. Constitution: no invariant violations, no zero-coverage criterion, no reference to a non-existent file/API.

- 2026-08-07 — **SDD Role Kernel (Track A, build item 1): spec SIGNED OFF and Stage-2 plan DRAFTED — PLAN-OK gate OPEN; ADR 0001 opens the new `tooling/sdd-roles` series (Proposed); `.sdd/binding.toml` gains its second ADR seam (C4).** The owner sign-off gate on `docs/sdd/specs/sdd-roles-kernel.spec.md` closed at plan kickoff: the sign-off review found **no plan-blocking findings** — all 26 CHK ids carry both a table row and an EARS criterion (verified 1:1), the A1 simplest-thing argument is made on the consumer list, and the corpus acceptance bar covers every check family, both dual-phase sub-rows, and all three exit codes. `docs/sdd/plans/sdd-roles-kernel.plan.md` derives the build: one Python ≥3.11 package (sole dependency `jsonschema`, C3), two console CLIs (`contract-lint`, `gate-wrap`), checks as a **data registry mirroring the CHK table** with a selftest-asserted registry↔corpus bijection (the "single enumeration, no second list to drift" clause made executable), a corpus-first WP order (WP0 scaffold → WP1 schemas+valid corpus → WP2 provenance cluster → WP3 static config/registry cluster incl. the cold-start CHK-REFS fixture → WP4 gate-wrap ∥ → WP5 self-integrity + the six item-2 DEFERRED stubs → WP6 docs + the clean-clone offline acceptance run). Plan-introduced G1 abstractions, each with the simpler-thing-rejected: the `selftest` subcommand as the gate executable (pytest would violate the single-dependency acceptance clause), sidecar `expect.json` annotations (embedded annotations would break closed schemas and case digests), the data registry (hardcoded call lists re-create the drift the spec forbids), and the descriptor-set `declares[]` field (a KernelConfig harness list would trip CHK-NEUTRAL). **Four mechanism-level readings returned for PLAN-OK ratification, recommended as drafted:** F1 — C3's {0,1,2} exit triple binds `contract-lint` fully while `gate-wrap`'s mapped exits are InvocationDescriptor data (its own usage errors = 1); F2 — CHK-PROTECT enforced as a `protected` object with 7 required named keys rather than literal `contains` constraints (identical invariant, no hardcoded workspace paths); F3 — harness declaration lives inside the InvocationDescriptor set document (`declares[]`, neutral `minItems: 3`), keeping every harness token inside ID instances so the CHK-NEUTRAL grep stays clean; F4 — `schema_version` starts at 1.0.0 with documented SemVer discipline. Per C4 the kernel ADR is drafted at plan time: **ADR 0001 “Adopt corpus-as-contract with a conformance-gated reference validator”** (`docs/architecture/adrs/tooling/sdd-roles/0001-…md`, Proposed → flips Accepted at PLAN-OK) records the load-bearing inversion — the golden corpus + CHK table is the normative contract, the Python implementation is a replaceable adapter, alternate-language ports are admitted only on identical-verdict corpus passes (the no-fork precedent generalized; o7's conformance-corpus pattern reused); rejected alternatives: implementation-as-contract, prose-only contract, N-native-implementations, per-harness contracts. Binding amendment applied: `adr_home_sdd_roles = "docs/architecture/adrs/tooling/sdd-roles/"` alongside the existing mobile-test-automation seam. **Gate: OPEN — PLAN-OK is the next human gate** (ratify F1–F4 or amend); on close, Stage 3 tasks (`sdd-roles-kernel.tasks.md`, EARS criteria mapped 1:1) then the Stage-4 analyze pass.

- 2026-08-01 — **ADR 0006 retention-class rider ACCEPTED — the last OPEN external dependency from the ASH Stage-3 plan is closed; `capture_run_edges.retention_class` = `CONSUMED | FORENSIC | PENDING_MINT`.** D4 (recorded on ADR 0010) had split the retention-class enum for ASH-Capture's `capture_run_edges` staging table out to a *named, required companion ADR 0006 rider, left OPEN* — the sole external item still gating the ASH build (it blocked only the `retention_class` CHECK cell, never the ScreenGraph migration, which D3 lifted). A 6-agent decision-first workflow (ground → 3 lenses: lifecycle-fit / A1-minimalism / compliance-mechanism → 2 adversarial skeptics: refute + constraint-safety → synthesis) produced the decision; **both skeptics' central findings were confirmed against source and folded in.** The core tension: `capture_run_edges` is disposable **conversion-state** (ADR 0006's "the schema line is the lifecycle line", 0006:82-84), *but* ADR 0014 requires some staging rows to survive the process for three named reasons (CRASH forensics 0014:389, parked `COMMIT_CONTENDED` commits 0014:390, escape-hatch human-time sessions 0014:391) — so "disposable" ≠ "delete at once", and the enum must express retention **within** the conversion-state purge boundary with **no value granting audit survival** (the durable copy of a successful run lives in `screen_graph_*`, outside the enum). **The enum — three values, one per distinct purge behavior the retention job can execute (A1 least-machinery test):** `CONSUMED` (staged edges minted into `screen_graph_*`; event-purged in-band by the mint transaction *after its final CAS commit* — because staged edges are re-read during the D3 CAS rebase up to the final commit, so the class encodes mint-*completion* not *intent* — plus a read-gated backstop that confirms the durable `screen_graph_versions` row exists before deleting, **never a TTL**); `FORENSIC` (terminal-failure runs committing zero graph rows; the one *eventless* value, so it reaps on a **bounded 30-day TTL** from a new `terminal_outcome_at` column, governed by CF6's no-silent-disable rule, deliberately shorter than any audit clock); `PENDING_MINT` (may still mint — mid-run, parked-contended, open escape-hatch; the retention job **never touches it**, another actor's event reclassifies it). **A fourth `PARKED_CONTENDED` value was rejected** — the retention job does the identical thing (hold, wait) to parked and escape rows, so the distinction is *who* reclassifies not *how* it purges (fails the purge-behavior test), and stuck-sweeper/park-liveness alerting is **already owned by D5/ADR 0007** (dated 2026-08-01, F-D5b/F-D5c) — a 4th value would re-own it. **Three owner forks ruled on the recommendations:** (1) the one unavoidable structural piece — a `terminal_outcome_at` timestamp (no timestamp column pre-existed; 0014:386 keeps the *outcome*, not a time) — ships in **A13's interim now** (alongside `retention_class`, both present-but-unconstrained), which keeps the rider's follow-up genuinely **one-cell** (add-CHECK + one-constant `FORENSIC` backfill, no `ADD COLUMN`) as new task **A13b**; (2) `FORENSIC` TTL max = **30 days**; (3) **keep `CONSUMED`** + its backstop (the scan predicate + audit surface proving in-band purge fired). **Constraint-safety audit PASS on all four axes:** does not reverse ADR 0006's base (every value dies inside the conversion-state purge boundary; the no-cross-lifecycle-FK rule re-asserted); does not weaken/re-own D4 (`retention_class` is a different column/axis from `dataClass`); strictly conversion-state (no value outlives certification); one-cell follow-up holds. **Compliance is unnumbered, F4-shape** — corrected from the synthesis's draft F7/F8 names, which would have collided (F7 is ADR 0004's, F8–F12 are ADR 0014's); per ADR 0006's own convention and the D3-amendment precedent (0012:209), the two new CI-blocking checks (CHECK-totality; read-gated one-directional purge) + the three-class retention-drill extension are ADR-0006-owned mechanisms arch-validate inventories alongside the F-series, not F-numbered. Recorded as an ADR 0006 `### amendment` before `## Notes` (Status unchanged, Accepted) mirroring the D3/D4/D5 house form. **Downstream flips applied:** ASH plan (status header, ADR-posture row, repo-tree note, §4 delegated-values, §5 WP1, §6 external-dep item 3, §7 gate — all OPEN→Accepted, "no external dependency remains OPEN"); ASH tasks (A13 ships two interim columns, new A13b one-cell follow-up + coverage-matrix row 12b); ASH spec (the `terminal-commit-only` retention clause resolved as a forward-reference annotation — WHAT unchanged, no re-sign-off — and the delegated-D4-values line flipped); spine tasks (Replan R1 D4 row + Lane-2 summary + Open-items section all → Accepted). **With this, every D1–D6 Lane-2 decision AND the ADR 0006 rider are DECIDED — the ASH build has no external blocker; the only remaining open thread is the S2 measurement spike (Lane 3, unblocked).** Full decision + amendment text: scratchpad `adr0006-rider-ground.md` + workflow `wf_10ae4510-b63`.

- 2026-08-01 — **ASH-Capture Stage-3 plan + tasks — GATE CLOSED, PLAN-OK (sdd-spec Stages 3–4).** Approved as drafted in one owner decision; three plan-gate forks ruled on the recommendations: (1) proposer = co-located two OS processes over the synchronous localhost channel (F8 assertions per-process; promotable to independent scaling later as a deployment change — 0014:539-544); (2) WP ordering = front-load the D-A/write core (WP-A4 arrival + WP-A2 write path) as the critical path to deliver Lane-3 S1 and unblock the S2 spike soonest, WP-A5 topology in parallel off WP-A0; (3) `capture_run_edges.retention_class` = ship the column present-but-unconstrained, enum CHECK a one-cell follow-up when the ADR 0006 rider lands. **Next: sdd-implement, WP-A0 (task-zero) first.** Drafting detail follows. With Replan R1 Lane 2 fully closed, ran sdd-spec Stage 3 (plan + tasks) for the ASH-Capture authoring arm against the SPEC-OK spec + Accepted ADR 0014. A 7-agent workflow (ground/index → 3 parallel design facets: architecture+G1 / work-packages / EARS→task-map → 2 adversarial verifiers: coverage + invariant → synthesis) produced `docs/sdd/plans/mobile-test-automation-ash-capture.plan.md` and `.tasks.md`. **Coverage verifier UPHELD_WITH_AMENDMENTS** (1 gap folded: the d3-gate second clause — spine Flyway V1 has no graph DDL — got its own task A05b `SpineFlywayScan` with a T02 doctored-V1 sample; and A44's compound five-criterion cell was decomposed into three isolated A44 assertions + two split into a new A45, so each criterion fails attributably). **Invariant verifier UPHELD, zero violations** — no F1 / ADR-0013 credential-isolation / ADR-0009 screening / S1 spine-untouched breach, least machinery preserved, every G1 abstraction justified. **All 61 atomic EARS criteria mapped 1:1** to tasks A01–A45 with explicit pass/fail (a criteria-count reconciliation was surfaced honestly: the brief said "65", the authoritative enumeration is 61 atomic EARS bullets — I independently verified 39 distinct *named* criteria spanning 61 bullet clauses, no criterion dropped). **Two owner decisions shaped the plan:** (1) standalone Maven reactor + two Spring Boot deployables consuming PUBLISHED `spine-contracts` + `screening` as versioned dependencies (reuse, never fork — cleanest F1 boundary); (2) graph work IN-SCOPE now (D3/D4/D5 Accepted lifts the gates; the `d3-accepted` marker is a mechanical CI predicate). The plan's seven G1 abstractions (two-process split; screen-gate component; deterministic validator + verdict token; `capture_run_edges` staging; per-backend signature rows; observation logs outside the content hash; CAS writer protocol) each carry a what-it-buys + simpler-thing-rejected. **Stage-4 analyze pass run:** grounding (five load-bearing ADR 0014 citations verified against source), invariant audit, and a cross-artifact consistency fix (the plan's design-decomposition WP0–WP10 numbering vs the tasks' build-execution WP-A0–WP-A6 grouping reconciled with an explicit lossless crosswalk table). **No new ADR** — ADR 0014 + the D3/D4/D5 amendments cover the design; the **ADR 0006 retention-class rider is the sole OPEN external dependency**, gating only the `capture_run_edges.retention_class` CHECK cell (WP-A1 ships the column present-but-unconstrained; the enum CHECK is a one-cell follow-up). **Gate: OPEN — the plan gate is the next human gate.** Three plan-gate forks returned for the owner: proposer deployment topology (recommend co-located two-process over the localhost channel, not an independently-deployed service); WP ordering (recommend front-load the D-A/write core WP-A2→A4→A5-write to unblock S1→S2 soonest); interim retention-class posture (recommend present-but-unconstrained column + one-cell follow-up migration, not blocking the whole migration). Synthesis source: scratchpad `ash-plan.md`/`ash-tasks.md`.

- 2026-08-01 — **Replan R1 D6 DECIDED — the last Lane-2 item: spine replay/device-run K = 1 RETAINED, a raise pre-registered as an event-gated CF6 entry criterion (first invocation of CF6's recorded-decision clause). Lane 2 (D1–D6) now fully closed.** An sdd-spec Stage-2 pass (6-agent decision-first workflow: 3 lenses — repro-audit / cost-value / lifecycle-fit — → 2 adversarial skeptics → synthesis) unanimously ranked **DEFER** first (Borda 9 vs KEEP 6 vs RAISE 3, all high-confidence) and it survived both skeptics *in hardened form only*. The decision: **K stays 1 for the weeks-0–3 spine** (pinning CF6's configured K at M10b's signed-off baseline, spec.md:270); a raise to the blueprint's flakiness-detection K (3 conversion / 5 certification, blueprint:77) is **not decided now** but pre-registered as a weeks-3–8 entry criterion gated on measured S2 evidence and routed through CF6. **A1/G1 cut both ways:** RAISE-now was rejected as over-machinery (a flakiness verdict with no consumer in weeks 0–3, at 3–5× device-minutes on an *unmeasured* pool whose pass-ratio is uninterpretable without S2's base-rate — the same un-grounded move blueprint:119 warns against); bare KEEP was rejected only narrowly (it drops the eventual raise into an unassigned lifecycle gap with no owner/trigger/gate, re-creating the evaporation risk). DEFER = KEEP + exactly one governance artifact (the falsifiable trigger) that makes the future raise auditable. **The sharpest objection both skeptics raised — the spec's own CF8 rationale calls "blocking later" the "promise most likely to evaporate" (spec.md:385) — is what the owner's forcing-function ruling had to answer.** The synthesis recommended a HARD block at the weeks-3–8 boundary; the **owner ruled SOFT checkpoint** on an explicit rationale: the phase boundaries are elastic (scope grew via ADRs 0014/0015 + the D3/D4/D5 gate, so Phase 1 may extend from 3→5+ weeks and the 3–8 window ~4 more), and a hard gate keyed to a *sliding calendar boundary* is not a real gate. **The anti-evaporation property was preserved by re-anchoring the forcing function from a DATE to an EVENT:** the K re-decision is gated not on "entering weeks 3–8" but on the **first-certification-verdict event** — no certification verdict may be issued while the CF6 K re-decision is un-taken (issuing a verdict at K=1 without re-deciding K against S2 would assert a flakiness posture never chosen). The schedule may slide freely; K cannot silently stay at 1 through the one moment it matters. Two further owner rulings hardened the trigger: **inconclusive S2 → hold K=1 with a bounded S2 re-run deadline** (recorded as its own CF6 decision — "inconclusive" is a pre-registered branch, not an indefinite-hold escape hatch), and **joint threshold authorship** — D6 (this decision) fixes the datum + the three branches (raise-forces / keep-justifies / inconclusive); the S2 spike owner sets the numeric flake-base-rate value against the real Perfecto pool (preventing both the "vague someday" and "guessed threshold" failures). **Honest residual (disclosed):** K=1 yields no flakiness verdict — weeks 0–3 are structurally blind to pool flake, an auditor sees only pass/fail; accepted because the spine proves the deterministic-replay substrate (flake-characterization is S2's job), certifies nothing, and the `ReplayReport` per-run outcome fields keep K-of-K derivable the moment K rises (the verdict is deferred, not the data). The un-closable-by-DEFER exposure — a determined slip of *both* S2 and the first-certification event — is bounded (event anchor + bounded re-run deadline + named owner), not eliminated; accepted knowingly in exchange for schedule elasticity. **Constraint-safe on all four axes:** does not reverse M10b (K=1 IS the baseline); does not weaken CF6 (it EXERCISES its recorded-decision clause for the first time); does not touch ADR 0014's discovery K (replay K only, distinct from the ≤K candidate-actions proposal breadth); adds no schema. Full decision text: scratchpad `d6-decision-final.md`. Recording: this entry (authoritative) + a Notes pointer on the CF6 row (spec.md:383) + the numbered flake-base-rate datum added to the S2 entry in tasks.md + walkthrough:272-273 traceability pointer. **With D6 closed, all six Replan R1 Lane-2 ADR-grade decisions (D1–D6) are DECIDED; the remaining open threads are the ADR 0006 retention rider (from D4) and the S2 measurement spike itself.**

- 2026-08-01 — **Replan R1 D3/D4/D5 DECIDED in one gate: three ADR amendments applied (ADRs 0012/0010/0007), all Status unchanged (Accepted); the ScreenGraph migration blocker is LIFTED.** An sdd-spec Stage-2 pass (7-agent decision-first workflow: ground → three parallel amendment facets → adversarial verify → synthesize; all three returned UPHELD_WITH_AMENDMENTS) closed the three interfaces ADR 0014 deferred by name — designed critically with pushback and purpose-justification, not rubber-stamped. **D3 → ADR 0012 (the hard build blocker):** a graph-version lineage chain amendment defines the chain ADR 0014's `lineage_digest NOT NULL` column presumed but 0012 never constructed — a per-`app_version` chain (never global, the same over-scoping the base decision rejects at 0012:104-105), a canonical field-sorted digest input (`app_version`+`prev_version_sha`+`graph_version_sha`+node/edge-set-digest, timestamps and `derived_from` excluded), a fixed genesis constant `"GRAPHCHAIN-GENESIS-v1"` so the `NOT NULL` contract holds at a root commit, and a chain-membership ruling (re-key/quarantine/promotion/`APPROVE_GRAPH_BASELINE`/`REJECTED_URL` rows are chained; the `screen_node_signatures`/`screen_edge_status` observation logs are ruled *out*, grant-protected append-only — the honest detect-not-prevent limit, stated not buried, with a periodic tail-digest recorded as a fast-follow). ADR 0014's graph grant stays ADR-0014-owned (F10) and is *inventoried-together, not re-owned*, so 0012's stated scope (lineage tables) is not silently widened. **Its acceptance lifts the ADR 0014 F10 gate: the ScreenGraph migration may leave the V1-no-graph-DDL hold on a mechanical committed `d3-accepted` marker** (0014:355-357, :797-798). **D4 → ADR 0010:** a prod-grade-data definition amendment turns ADR 0014's undefined "prod-grade" adjective into a decidable **provenance predicate** — SYNTHETIC vs PRODUCTION_DERIVED (masked production data is PRODUCTION_DERIVED; deny-by-default when unattested; one manifest covers corpus AND environment) — so `dataClass` becomes machine-checkable parallel to `envClass`; a PRODUCTION_DERIVED attestation *both* suspends discovery at runtime (filed as an incident record) *and* reopens ADR 0010 as a governance event (0010:147-148 already carried the matching flip trigger, which is why D4 routed here rather than to a new ADR). The retention-class enum for `capture_run_edges` (0014:363-367) is **split out to a named companion ADR 0006 rider, left OPEN** — this ADR owns the provenance line because it owns the flip trigger; ADR 0006 owns retention because retention is lifecycle. **D5 → ADR 0007:** an ASH-Capture graph-mutation ruling executes 0007's own deferral pointer and strengthens (adds/moves/reverses no seam) the two-seam invariant — Ruling 1: ScreenGraph mutations are synchronous same-local-transaction writes (ratifying ADR 0014 D-D's rejection of the walkthrough §13.6 outbox routing as a positive 0007 ruling, so the invariant now lives in both ADRs it constrains); Ruling 2: the `COMMIT_CONTENDED` park-and-sweep buffer is **permitted** as a non-precedential intra-process-family retry over `capture_run_edges` on two structural bounds (single-owner + non-precedential); Ruling 3 (the verifier's load-bearing correction): fail-loud-and-recapture is conformant **only** for deterministically re-derivable re-verification results — quarantine/BROKEN commits are human/screening-originated and *not* replay-derivable, so they must park or fail-loud-with-incident, never silently recapture. Per the owner's fork resolutions: the buffer is permitted but **defaults to fail-loud**, and the keep/kill choice is **S2-evidence-gated on a measured graph-commit CAS-contention rate** now added to S2's pre-registered thresholds (determinism-disposes discipline applied to an infra choice). Two low-stakes clarify forks took their recommendations as applied — obs-log integrity is grant-only for the initial build (tail-digest a recorded fast-follow), and D3's chain binds to ADR 0014's current serialization with any D5 change to graph-write ordering recorded as a D3 re-open trigger. Seven consolidated ASH-design improvements were surfaced (dataClass symmetric with envClass under one F11 startup assertion; one-provenance-root manifest spanning corpus AND environment; inventory the 0012+0014 grants as one drift set; unify the two per-release detective recomputes into one non-racing pass; the genesis constant; a back-reference from ADR 0014 D-D to the dated 0007 ruling; and the CAS-contention datum in S2) — recorded for the ASH Stage-3 build, not applied to the base ADRs here. **New open item: the ADR 0006 retention-class rider** (`capture_run_edges` retention stays blocked until it lands — it does not block the graph migration, only the staging table's lifecycle attestation). Full routing: `docs/sdd/plans/mobile-test-automation-spine.tasks.md` §Replan R1 (D3/D4/D5 rows → DECIDED, Lane 3 S2 threshold extended, Open-items section added).

- 2026-08-01 — **Replan R1 D2 DECIDED: A0 LLM normalizer DEFERRED, deterministic canonicalization FOLDED (ADR 0015 Accepted).** An sdd-spec Stage-2 pass (6-agent decision-first workflow: ground → 3 independent lenses → adjudicate → design) returned a unanimous FOLD_INTO_A1 at high confidence across security, value, and lifecycle lenses; RATIFY was the only disposition the evidence actively refuted. `0015-defer-the-llm-normalizer-a0-and-fold-deterministic-canonicalization-into-ingestion.md` records: A0-as-an-LLM-stage deferred (evidence-gated re-open on a measured A1 parse-failure rate — M16 corpus + a real free-form-English sample, decomposed fold-fixable vs A2-fallback-served); a minimal committed phrase-canonicalization table + noise-strip ruleset folded into the M15 per-adapter canonicalization surface (NOT A1's structure-only contract). The owner reviewed the A0 mock trio and named three reasons (messy input, screen-context, consistency), decomposed at the gate: consistency → the deterministic fold; screen-context → scope-ruled to A2 (already an LLM-fallback inside the screened seam), no new surface; messy-prose → the deferred re-open trigger. **Flip-counter ruling as settled fact: A0 trips no fourth flip** — both its flows are second paths into already-screened ADR 0009 classes (1) and (3), mirroring ADR 0014's CS4 precedent; the review's "2/3→3/3 territory" framing predates ADR 0014's amendment and is stale. Spine spec carries an A0-fold amendment (criteria A0-1..A0-6, re-closed at the gate as the post-A0-fold baseline); walkthrough §2.0/§8/§9/§12 demoted A0 to DEFERRED. Placement fork resolved FOLD-now (DEFER-only fallback retained if adapter-contract review finds the table non-minimal; never RATIFY). No spine task invalidated; the fold lands inside T17/T18/T19 with guards in T02/T22/T24.

- 2026-07-31 — **Combined gate CLOSED: SPEC-OK on the ASH-Capture spec + ADR 0014 Accepted + riders ratified.** One owner decision closed the Stage-2 gate: `mobile-test-automation-ash-capture.spec.md` SPEC-OK; ADR 0014 Proposed → Accepted; the ADR 0001 seam-vocabulary amendment (`ObservationPacket`/`CandidateActionSet` join the Invoke Models crossing vocabulary) and the ADR 0009 ASH-Capture flip amendment (counter executed at 3 of 3, ASH-repo-scoped screen-gate promotion, spine-side promotion recorded open and routed to the spine's own change process) both recorded in their home ADRs with Last-modified lines; the A11 stored-biometric override ratified (human-confirmed at clarify C2). Consequences: Replan R1 Lane-3 S1 (signature re-keying design) is delivered, the S2 measurement spike is unblocked with pre-registered pass/kill thresholds, and the next human gate is the Stage-3 plan gate for the ASH repo.

- 2026-07-31 — **SDD Stage 2 run on Replan R1 D1: ADR 0014 drafted + ASH-Capture spec drafted, clarify pass complete.** A 10-agent workflow (ground → 4 design facets → 4 adversarial verifiers → synthesis; all facets UPHELD_WITH_AMENDMENTS, 36 amendments applied) produced `adrs/application/mobile-test-automation/0014-confine-the-llm-to-proposing-in-ash-capture.md` (Proposed — LLM proposes only; deterministic arrival/re-keying decision table, two-process proposer/executor split, backend-scoped signatures, CANDIDATE-at-birth graph writes with fork-unrepresentable CAS, deny-by-default deep-link/TYPE admission, ADR 0009 flip ruled 3-of-3, §13 Postgres storage adopted; D3/D4/D5 held as named interfaces) and `docs/sdd/specs/mobile-test-automation-ash-capture.spec.md`. The five clarify forks were resolved by the owner same day (C1 auto-RE-KEY on strict path; C2 A11 stored-biometric override confirmed + lease fallback accepted as recorded risk; C3 ASH-scoped 0009 flip, spine question routed onward; C4 APPROVE_GRAPH_BASELINE blocking at certification boundary only; C5 quarantine recall = flag-and-cross-reference, no retroactive invalidation). Open: combined SPEC-OK + ADR-acceptance gate, which also ratifies the ADR 0001 seam-vocabulary and ADR 0009 flip amendments.

- 2026-07-31 — **Replan R1 (sdd-replan) APPROVED — REPLAN-OK.** Trigger: the `o1-pipeline-walkthrough` (A0 Normalizer, ASH-Capture, K=3/5, Postgres ScreenGraph) plus a 7-agent critical review (28 CONFIRMED findings — `docs/research/o1-pipeline-review.md`). Verdict: spine board T01–T43 all STAY, zero tasks invalidated — the signed-off spec/plan remain normative; the contradictions live in the walkthrough. Routing: Lane 1 editorial fixes E1–E6 applied to the walkthrough same day; Lane 2 ADR-grade decisions D1–D6 (ADR 0014 decide-list incl. signature re-keying + proposer/executor split, A0 ratification with ADR 0009 screening, ADR 0012 graph-chain amendment, ADR 0010 "prod-grade data" definition, ADR 0007 ruling, K policy) routed to sdd-spec/arch-decide; Lane 3 measurement spike blocked on the re-keying design (D1) with pre-registered kill thresholds. Full record: `docs/sdd/plans/mobile-test-automation-spine.tasks.md` §Replan R1.

- 2026-07-29 — **Standalone single-file edition of the design pack generated.** `spine-design-pack-standalone.md` (~1.4 MB) embeds all four diagrams as base64 data URIs, inlines the P1–P3 `view.md` node/edge tables as appendices A1–A3 (satisfying each view's export caveat — the honesty tags travel with the file), and flattens repo-relative links to plain repo paths, so the file is shareable outside the repo with zero sibling dependencies. Built deterministically by `build_standalone.py` in the same folder; regenerate after any deck edit or diagram re-render, never edit by hand.

- 2026-07-29 — **P4 delivery-roadmap view added to the design pack; the deck's mermaid roadmap retired.** The page-10 work-package flow in `spine-design-pack.md` is now a D2 view (`p4-delivery-roadmap.d2` + `@2x.png` + grayscale proof), rendered through the same `d2` → `rsvg-convert` toolchain as P1–P3 so every visual in the pack is D2. It is a delivery flow, not a C4 view, so it carries no IR and the architecture linter does not apply — the SELF-AUDIT records the discipline kept anyway (dependencies verbatim from the plan's WP table §5, module colour-tracking against P2, hexagon for the screening library, heaviest stroke on the WP7 gate). P1–P3 re-linted the same day: all PASS, 0 failures.

- 2026-07-29 — **High-level design pack added under `presentations/mobile-test-automation/`** — a communication artifact for the chief-architect / delivery-manager / developer buy-in audience, derived entirely from the frozen baseline (spec post-P1 2026-07-27, plan PLAN-OK 2026-07-28, ADRs 0001–0013); nothing re-decided, open items kept tagged (ADR 0011 probe, PostgreSQL working assumption, M16 floor). Deck-style markdown (`spine-design-pack.md`, one idea per page: shared pages 2–5, architect 6–8, delivery 9–11, developers 12–13, the ask on 14) plus three new presentation views authored via the generating-architecture-diagrams skill — P1 spine context, P2 module map, P3 replay flow — all lint-PASS on first render (13/13, 14/14, 21/21 labels verbatim; 58 relocated facts landed), grayscale proofs and SELF-AUDIT alongside. The delivery-roadmap page is deliberately a mermaid flowchart inside the deck (work-package flow is not system topology). The nine-view engineer-grade set at `docs/mobile-test-automation-diagrams/` stays authoritative; the presentation views declare their `opens`/`omitted` pointers into it.

- 2026-07-27 — **Pass P4 (Verifiability) phases 1–2 complete; the row lands at 6/9/9/6/9, sum 39 — the highest-standing row in the matrix.** The second pass run fully post-mitigation: five blind lens agents scored residual risk against E1–E6, D1, M1–M43, S1–S27, CF1–CF11, ADRs 0001–0013 and the post-P1-baseline spec, with the P3-adjudicated External 9 disclosed as prior state movable only with recorded reasoning. Discipline held again — no illegitimate re-raises; every confirm landed at its adjudicated residual (CF6 at 4, CF7 at 4, M24 at 4, S10 at 6, perfecto:ai at 6). 44 raw rows merged to 10 shared clusters plus 8 single-lens finds, an 18-entry register. **The headline is a structural blind spot, not an eroded control:** three mitigation passes made the certification *verdict* nearly incorruptible (CF1 custody, CF6 K-integrity, ADR 0012 hash-chained lineage, M18 no-silent-disable), but the criterion's opening clause — "no generated test enters the regression suite without passing objective gates" — has no enforcement mechanism in any artifact: suite membership is a normal Git commit, invisible to F1–F7, cheaper than eroding any threshold, and leaving no gate record to argue over. Two lenses found it independently at 3×3 (P4-1); the named fix is one CI-blocking membership fitness function (every suite test references a verdict ID). The second pattern: the calibration record anchoring the subjective gate is softer than everything it anchors — labels self-graded by the reviewers who steered the conversions, no held-out split from the corpus that seeds exemplars, and a TPR/TNR figure no auditor could re-derive (P4-4, P4-13). Five arbitrations, all ruled at the pass recommendation: the suite-admission 9 admitted and landed in V&C by failing function rather than vector (what fails is admission, certification's own job; the commit is the route); the Conversion calibration cluster at the median 6 under the unsure rule; both advisory-judge facets at 6 — facet (b) ruled on impact per the failed-measure precedent, an undefined admit-over-FAIL rule for a top-3 conjunct being a failed measure, not a degraded one; the four threshold-governance insufficiency raises admitted at submitted scores (initial values born low, the confidence floor absent from CF6's enumeration entirely, verdict pins stopping at K, change authority undefined) with CF6's own confirm staying 4; and all eight single-lens finds admitted (headlined by OPS's per-verdict ENV_INFRA exclusion silently filtering the zero-flakiness sample, SEC's redaction-success corrupting gate inputs with no redaction-delta lineage field, and COMP's calibration-event-not-evidence-grade find). The E&P automatic 9 extends M22's held state to this row via CF1's custody precondition; External unanimously confirms the P3 cell riding M5's inquiry. Reveal canvas: `p4-verifiability-risk-reveal.canvas.tsx`. Phase 3 (mitigation of the 18-entry register) pending; P5 remains queued.

- 2026-07-27 — **Combined gate closed: the P1 spine-spec edit pass applied and re-signed-off, ADR 0012 and ADR 0013 Accepted, the ADR 0011 M39 amendment ratified — one approve-all decision.** The owner directed a departure from the one-at-a-time review used for the P3 and P2 edit passes: the P1 fold-in was applied as a batch and everything the pass owed was taken at a single gate, recorded as such in the spec's edit-pass section rather than left implicit. The spec (`docs/sdd/specs/mobile-test-automation-spine.spec.md`) gained nine new failure-path criteria (schema-enforced individual principals with per-component service accounts and no catch-all `system`, M37; grant refusal + CI assertion with supersede-not-update correction semantics and the per-conversion chain link committing in the same transaction, M38/ADR 0012; retention-class required at landing, M39; screening on hierarchy-capture output and pulled artifacts under the two-half F3 construction, screened-fixture CI markers, and quarantine-and-review with recorded overrides, M35; credential indirection with the warn-only secrets scan labeled as the owner's ruling and CF8's dated flip, M34; ephemeral-only dev/CI data directories, M40), happy-path amendments (screening at capture and at landing; the device-gate worker holds no gateway credential from the first commit — ADR 0013's shape made spine-real; chain-head anchoring with stale-anchor alerting; the M36 novelty-sampling flag and corpus provenance-mix reporting; short spine object-lock retention with crypto-shredding explicitly deferred to before the first audit-retained artifact), two new task-zero wires (the ADR 0012 grant assertion and the warn-only secrets scan joining F1–F4), and four new carry-forward rows — CF8 (vault-key indirection, warn→blocking as a dated weeks-3–8 *entry criterion*), CF9 (advisory judge, never autonomous), CF10 (execution isolation per ADR 0013, its spine provision being the worker's credential topology), CF11 (auditor-export inheritance, exports as attributable lineage events). ADR 0012 flipped Proposed → Accepted with no blocking input; ADR 0013 flipped with its short-lived-session-credential assumption accepted as recorded (fallback named, not verified first); both carry ADR 0010 security-review queue entries running as parallel work. ADR 0011 itself stays Proposed pending the week-0 platform probe — only its amendment was ratified. The spec is now the **post-P1-mitigation baseline**, ready for plan + tasks; stage-5 passes P4 (Verifiability) and P5 (remainder) remain queued.

- 2026-07-27 — **The three artifacts P1 phase 3 owed are drafted: ADR 0012, ADR 0013, and the ADR 0011 erasure amendment.** **ADR 0012 (tamper-evident lineage, Proposed, no blocking input)** — per-conversion hash chain with chain heads anchored into immutable object storage through the ADR 0011 port. Anchoring is named the load-bearing half rather than a nicety: a chain alone proves internal consistency to whoever controls the database, which is the wrong audience, whereas anchors under object lock mean a database-side rewrite cannot rewrite the evidence of the rewrite — and it costs an interval job with no key material. Five alternatives compared on a weighted matrix (chain-only, grants-plus-DB-audit-logging, vault-key signing, external notarization): the two cheap ones share the fatal property that detection depends on the platform being audited; signing buys authorship proof at the price of key management plus a dependency on the still-unnamed vault; notarization buys maximum independence with a new external surface on a system stage-5 has spent two passes bounding. Chain scope is the conversion, so chaining adds no contention the domain does not already have, and the digest joins the existing write transaction so ADR 0007's same-transaction invariant holds and a partially chained lineage is not representable. Two consequences recorded that a reader would otherwise have to derive: it **detects rather than prevents**, and it is **exactly as strong as ADR 0011's object lock** — which makes object lock a hard requirement on the probe's binding rather than a preference. One interaction checked deliberately before adopting both mechanisms: crypto-shredding destroys an *object's* readability and alters no row, so erasure and tamper-evidence do not collide. Correction semantics (supersede, never update) drive the timing — the first lineage migration, not "before release" — and constrain every future lineage reader. **ADR 0013 (generated-code execution isolation, Proposed)** — the response to M42's finding, with the reasoning that E3 cannot dispose of it recorded in the Context: the internal-network fact cannot dismiss the threat model an accepted ADR is built on, so invoking it at the execution step while relying on the untrusted-input premise at the generation step would be incoherent. Chooses to **remove the prize rather than build the cage**, on the stated ground that impact reductions survive the discovery of a new escape technique while likelihood reductions do not — no long-lived credential in the execution context, no gateway credential at all (a correction more than a mitigation: executing a script needs no model access), a single-run device session token, separate-process execution committed as a *shape* so every sandbox option stays open while the retrofit becomes impossible, and static capability rules declared a supplement because allowlist-shaped analysis of generated code is false-negative-prone by nature. Five alternatives compared, including per-script human review — effective and fatal to the system's purpose — and trusting the screening library, which would make three call sites the single point of failure for credential compromise on a boundary ADR 0009 already calls structurally invisible. Three honest flags: short-lived device session credentials are an **unverified vendor capability** with its fallback named (the deferred sandbox promotes from "later" to "required and stronger") rather than filed as a blocker, since the decision direction is unchanged either way; deferring the sandbox technology is a real bet on weeks 3–8 having schedule, survivable only because of the shape commitment, with a dedicated unprivileged user as the defensible floor; and the red-team corpus gains a case class it never had — injection *through* generation *into* execution — now assignable because M36 gave the corpus an owner. Its consistency with M41's advisory judge is noted as deliberate: no autonomous authority for machine output. **ADR 0011 amended (M39)** with the erasure-under-immutability section naming the collision the P1 pass created — this ADR mandates write-once objects and ADR 0012 mandates append-only chained rows, while erasure is equally mandatory — resolved by envelope encryption under per-conversion keys with crypto-shredding as the erasure act, key destruction marking affected verdicts `EVIDENCE_DESTROYED` (because erasure destroys the reconstructibility CF1 and CF7 depend on, so the design records which property was given up, when, and by which principal), a break-glass incident path for lineage-resident PII that shredding cannot reach, and a timing rule that bets on spine evidence being disposable — stated as a bet, since retrofitting encryption onto immutable objects has no cheap path and the bet fails if spine evidence is later reclassified as retained. Compliance across the three gained the anchor-bucket immutability check, an erasure-integrity check, a session-scope assertion, credential-absence and process-separation assertions (the latter in ADR 0009's two-half construction, since a dependency rule proves structure and not behavior), and restore-drill extensions covering chain verification and a rehearsed crypto-shred. All three await the architecture gate; the P1 spine-spec edit pass and passes P4/P5 remain.

- 2026-07-27 — **P1 phase 3 (mitigation) complete; all 21 Security & privacy entries adjudicated (E6 + S23–S27 sweep + M33–M43); the row drops from 6/6/6/6/6 to 4/4/6/4/6, sum 30 → 24.** Triage recorded P1 as structurally unlike P2 and P3: only five of twenty-one entries rode anything already in flight, so this was the pass that *designed* security machinery rather than re-scoring it against evidence. Accepted shape — a five-entry sweep plus **ten mechanism-grouped rulings** over sixteen entries, grouped because nine collapsed onto two things already decided to exist (ADR 0009's screening library and principal identity on written rows), with the honest cost stated: grouping risks averaging over entries whose exposure differs, so every grouped item enumerated its entries and scored each separately (the M10 pattern). New evidence fact **E6** — the bank has an existing secure-SDLC / security-controls standard this system inherits — ruled to re-score nothing by itself, entering each ruling as a per-entry input, because a blanket likelihood cut on an unread document is the determinism theater M19 penalizes; the asymmetry was recorded too, that the read can *raise* week-0 scope as readily as lower it. **M33** is the enabling probe: one controls-baseline read against six checklist questions (secrets, identity, data classification, retention/deletion, model risk, and — added at the M36 gate — an inheritable injection/PII corpus). **Three structural design commitments came out of the pass, cheap only because they were made before code exists:** **M42** credential isolation from the generated-code execution context — the pass's strongest finding, that ADR 0009's untrusted-source-text premise and P1-15's unsandboxed credential-holding worker are two ends of *one* attack path, and E3 cannot be invoked to dismiss the threat model an accepted ADR is built on; the fix removes the prize (single-run session token, no gateway credential) rather than building the cage, with static-gate rules and a separate-process shape requirement as supplements, spinning out as **ADR 0013**. **M41** the fidelity judge ruled **advisory to a human certifier, never autonomous** — decided on M37's grounds (attributable human approval was just made load-bearing) more than on model-risk grounds, with the throughput tension recorded rather than papered over, plus a model inventory stating "MRM applicability unverified" on its face. **M39** crypto-shredding as the erasure mechanism, naming the collision this pass created: items 3 and 6 make evidence deliberately immutable while erasure is equally mandatory, and key destruction is the only reconciliation — with the consequence made explicit, that erasure destroys the reconstructibility M6 and CF7 depend on, so key destruction **marks affected verdicts evidence-destroyed** rather than leaving them looking valid; timing ruled on the sharper line (design now, build before the first *audit-retained* artifact, keep spine object-lock retention short). Also: **M34** corrected a phase-2 premise at the gate — the vault arbitration rested on the spine's reference test needing no secret, the owner confirmed it *does* authenticate, so facet (a) was re-priced upward before being mitigated back with credential indirection by construction; the secrets scan was ruled warn-only for the spine with a **dated** blocking flip, and the 4-instead-of-2 cost of that choice recorded. **M35** amended ADR 0009 to define its boundaries by data class rather than named component, reconsidered and again declined the flip to a dedicated component (the ADR's own matrix favours the component, but a dependency edge proves availability not invocation, so F3's runtime half stays the whole guarantee either way) and recorded the **flip counter at 2 of 3** with a third path forcing the decision; the Acquire UI Evidence call site was ruled into the spine because it is the only technical chokepoint the Copilot egress will ever have. **M36** substituted the worksheet's *manual sampling* measure — random sampling against a near-zero base rate cannot challenge a zero — with corpus regression plus novelty-based sampling, gave the red-team corpus an owner (the security function, over the owner personally, avoiding a third concentration), replaced an undefinable adequacy floor with provenance-mix reporting, and recorded the irreducible residual that an operational feedback loop cannot generate blind-spot cases. **M37** found a *defective worksheet measure*: "zero anonymous rows" is satisfiable by a shared login, restated as individual-principal attributability, with schema-enforced principals, per-component service accounts (closing the catch-all `system` trap the proposal itself created), and review-UI SSO as a hard requirement. **M38** hash-chained lineage with chain-head anchoring into immutable storage, spinning out as **ADR 0012**. Five entries were deliberately **held** rather than credited (P1-16, P1-20, P1-4, P1-19, P1-7); M37 and M38 were applied on skipped questions and marked as such. Two cells hold at 6 for reasons outside design's reach — retention enforcement riding M33 Q4 and ADR 0011, and the two unread vendor documents, stated at the sweep gate as vendor-management work. Owed next: ADR 0012, ADR 0013, the ADR 0011 crypto-shredding amendment, the P1 spine-spec edit pass (ten spec-routed rules plus four carry-forward rows), and passes P4/P5.

- 2026-07-27 — **Pass P1 (Security & privacy) phases 1–2 complete; the row lands at 6/6/6/6/6, sum 30 — the first storming pass run fully post-mitigation.** Five blind lens agents scored residual risk against evidence facts E1–E5, decision D1, mitigations M1–M32, sweeps S1–S22, carry-forwards CF1–CF7, ADR 0011 and the post-P2-baseline spec, with re-raising a mitigated risk named a defect. The discipline held: all five lenses landed the three routed inputs at their adjudicated residuals (Perfecto residency 6 unanimous riding M1's open MSA/DPA read, gateway egress 2 unanimous per E4, the ADR 0010 control at 4 from four lenses), and no lens re-raised a mitigated risk — the E-facts and M-series did their job. 55 raw rows merged to 12 shared clusters plus 9 single-lens finds, a 21-entry register. The headline pattern is new: the stage-1 worksheet names security *machinery* — the test-data vault behind the top-3 "vault key, never literal value" measure, redaction-at-capture, manual sampling against detector false confidence, retention deletion — that no ADR, spec criterion, or mitigation designs, builds, or owns; the measures exist, their mechanisms don't. One automatic 9 fired (operations: vault technology/owner UNKNOWN — the M22 shape). Five human arbitrations, three ruled below the pass recommendation and recorded as such: the vault automatic **overridden to 6** (ruled weeks 3–8 machinery — an unowned dependency, not an unknown critical technology of the spine; the week-0 vault-naming probe stays the obvious phase-3 candidate), R-in 3 held at the four-lens 4 (delivery's drain-enforcement insufficiency noted on the entry for phase-3 pricing), pull-path redaction ruled likelihood 2 (entry 4 — the egress detector and E2 bound the harm window), the auditor export ruled 4 (distance to build plus the queued review), and all nine single-lens finds admitted at submitted scores (P2/P3 precedent). The External cell confirms the P3 column's adjudicated 6 rather than moving it. Reveal canvas: `p1-security-privacy-risk-reveal.canvas.tsx`. Phase 3 (mitigation of the 21-entry register) pending; P4 and P5 remain queued.

- 2026-07-27 — **ADR 0011 drafted (Proposed): evidence object storage behind an S3-compatible port, production binding decided by the week-0 platform probe.** The vehicle M22 named for the P2 register's automatic 9, with M23's two backup/restore rules folded in as the ADR's own section (restore-ordering invariant — object store restores at or after PostgreSQL's point, sound because the mandated write order means references never point forward in time; post-restore custody reconcile reusing CF1 machinery with M9/M15 digest matching; object immutability stated as the invariant's precondition). The decision splits what is safe to decide now from what the probe decides better: the *port* is decided (no evidence write outside it, dev/CI on containerized MinIO from task zero — the D1 analog), the *binding* is blocked on the probe (internal S3-compatible platform service if one exists, inheriting durability/backup/on-call; otherwise self-operated MinIO with object-lock immutability), and the self-operated branch is the **recorded default** if the probe hasn't answered by the last responsible moment — before the first real evidence artifact lands (~week 2) — so silence has a defined outcome instead of a filesystem stopgap leaking into the retention design. Honest flags recorded: the probe branches are asymmetric in real cost (the port makes them cheap to swap, not equally expensive to live in); dev-grade MinIO could pass the week-3 gate because M19 covers input grade, not storage grade — guarded by one plan-level line (production binding live before the first real artifact); MinIO's AGPL license rides the probe as an OSS-catalog check. Compliance: a port-only ArchUnit-style dependency rule (F1/F2 construction, inventoried by arch-validate), an immutability assertion at deployment, a quarterly restore drill joining ADR 0006's retention drill, and the ADR 0010 security-review queue entry.

- 2026-07-27 — **P2 batch spec-edit pass complete: the four field rules plus two carry-forward rows folded into `docs/sdd/specs/mobile-test-automation-spine.spec.md`, each reviewed and decided one at a time by the owner (all six accepted as proposed); the spec's sign-off gate re-opened per its own condition and was re-closed by human re-sign-off the same day — the spec is now the post-P2-mitigation baseline, ready for plan + tasks.** The M24 record-actual rule landed as three edits: a new blocking failure path (a device run whose actually-reported context mismatches the requested set on any pinned facet quarantines with an alert — the M10a status shape, never silently counted), actual-vs-requested execution-context fields with an explicit delta on the device-gate lineage clause, and hierarchy-capture pool provenance with an off-pool flag whose weeks-3–8 consumer (certified locators require pinned-pool provenance or a recorded decision) rides as a rider on the carry-forward section. M28 amended the F6 pinning criterion: gate-run lineage rows carry the CI-runner environment (runner-image digest plus JDK/Maven/tool versions) and the runner image is pinned by digest, never a floating tag. M30 locked the prompt-version field's future value-space to the prompts-repository Git commit SHA — content-addressed by construction, labels and tags never valid. M32 added a ubiquitous criterion: every CLI/tool emission records its build-time Git SHA in the lineage rows it writes. CF6 (K-integrity: K-configured vs. K-executed in verdict lineage, certify refuses under-K verdicts, threshold changes are recorded decisions) and CF7 (fidelity re-derivation via deterministic cache replay, with verdict-referenced cache entries becoming retention-bound — the honest cost being that M27's "allowed to lose data" cache gains a second row class) joined the carry-forward table, now seven rows with the MUST-import obligation on the weeks 3–8 spec. Still owed from stage 5: ADR 0011, the two week-0 asks, the ADR 0002 Notes line (M27 cache schema), and passes P1, P4, P5.

- 2026-07-27 — **P2 phase 3 (mitigation) complete; all 24 Reproducibility-register entries adjudicated (D1 + S12–S22 sweep + M22–M32); the row drops from 9/9/9/6/9 to 4/4/9/4/9.** Triage split the register on human ruling into an eleven-entry covered sweep — entries already served by the P3 M-series, evidence facts, or spec criteria (gateway self-report folded into M5's held 9; Copilot pinning re-scored 4 on M12's schema honesty; five more re-scored on M6/M8/M10a/M15/M18/M21; two closed at 2) — and thirteen uncovered entries run as eleven adjudications. The storage pair went first per the ruling: **M22** the object-storage automatic 9 held, with a three-layer collapse mechanism accepted — a week-0 platform probe (does the bank already run an internal S3-compatible service?), a containerized-MinIO interim behind an S3 port (the D1 analog), and the technology decision spun out as **ADR 0011** with its last responsible moment before the first real evidence artifact lands; **M23** cross-store restore-point incoherence re-scored 9→4 on a restore-ordering invariant (object store restores to a point at or after PostgreSQL's — sound because the mandated write order means references never point forward in time) plus a post-restore custody reconcile, both landing in ADR 0011's backup/restore section. M22–M24 rulings landed as recommendations applied on skipped questions — recorded as such, per the M10a precedent; M25–M32 were accepted explicitly as a reviewed batch. **M24** grouped the Perfecto execution-context-drift 9 with its two facets under a record-actual rule (actual session context recorded beside requested, pinned-facet mismatches quarantine, hierarchy captures carry pool provenance with the M8 doesn't-count shape) — drift held at 6 on the M1 lifecycle answers, with the honest residual stated: recording makes drift visible, not impossible; retired devices cannot be re-run. **M25** K-run erosion 9→4 as carry-forward **CF6** (K-integrity: pinned K, K-configured vs K-executed in verdict lineage, certify refuses under-count, threshold changes are recorded decisions). **M26** the PostgreSQL-swap 6→4 on the week-0 catalog probe, D1 having removed the schedule pressure. **M27** the ADR 0002 response cache gets its owning store — a fourth PostgreSQL schema, evictable and rebuildable. **M28** CI-runner pinning closed at 2 (image digest + toolchain versions in gate-run lineage). **M29** fidelity re-derivation as carry-forward **CF7** — verdicts bind to the cached judge response so re-derivation is deterministic cache replay. **M30** prompt version defined as the prompts-repo Git commit SHA, closed at 2. **M31** the auditor-export find closed by reference — ADR 0008 had already assigned it to Preserve Provenance. **M32** CLI build identity embedded at build time and recorded in lineage, closed at 2. Two 9s stand on the whole register: the gateway version contract (M5's inquiry) and object storage (M22's probe/ADR). Owed next: the P2 batch spec-edit pass (M24/M28/M30/M32 field rules + CF6/CF7 joining CF1–CF5, one gate re-open/re-close), ADR 0011, the two new week-0 asks, one ADR 0002 Notes line. Remaining passes: P1, P4, P5.

- 2026-07-27 — **P2 phase 3 (mitigation) opened; decision D1 recorded: interim primary-store posture is embedded/containerized PostgreSQL.** While catalog approval for a provisioned instance is pending, dev and CI run the real PostgreSQL engine via Testcontainers or Zonky embedded-postgres — zero dialect drift on the SQL the spine most depends on (JSONB, `FOR UPDATE SKIP LOCKED`, the transactional outbox), with Flyway-style versioned migrations from task zero so cutover to the approved instance is a connection-string change plus migration replay; a shared pre-approval environment, if needed, is a PostgreSQL container on an internal VM inside E3's boundary. Alternatives rejected with reasons in the risk report (H2 PG-mode dialect drift, SQLite's single-writer model against a two-writer seam, local JSON files breaking the ADR 0007 same-transaction invariant, pure in-memory losing lineage on restart). Explicitly not hedged: a catalog mandate for a different engine — that residual stays priced on the register's PostgreSQL-swap entry. Routes to the plan (task-zero provisioning); C3's working assumption in the spec is unchanged, so the sign-off gate stays closed. Triage of the 24-entry register into a covered sweep vs. uncovered one-by-one adjudications follows.

- 2026-07-27 — **Spine-spec edit pass complete: all eleven spec-routed P3 mitigation rules plus three riders folded into `docs/sdd/specs/mobile-test-automation-spine.spec.md`, each reviewed and decided one at a time by the owner; the spec's sign-off gate re-opened per its own condition and was re-closed by human re-sign-off the same day — the spec is the post-mitigation baseline.** Seven rules landed as EARS criteria directly: hash-at-pull (M9, blocking failure path — evidence without a landing digest does not exist), hash-at-ingest with per-adapter canonicalization (M15, accepted in the stronger form: the digest rides in the IR *and* the canonicalized snapshot is stored in object storage for audit reconstruction), queue hygiene on the ENV_INFRA re-queue (M21 — bounded cap, backoff, persisted quarantine record shaped like the future review-queue record), the `corpusClass` lineage field plus a second binding clause on the week-3 gate itself (M19 — the acceptance bar now requires `REAL_INGESTED` ingestion evidence; the replay leg stays hand-written by design), dual-fixture contract tests bound to the shared ingestion contract rather than to the adapters (M20 — deferring an adapter cannot remove its fixture family), task-zero F1–F4 CI wiring as an ordering constraint (M18 — a build without fitness functions wired is not a valid baseline), and quarantine-unknown as a classification *status* distinct from the seven taxonomy classes rather than an eighth enum value (M10a, the recommendation applied when the owner skipped the question — recorded as such), paired with Smart Reporting contract tests (S2/S3). The four certification-stage rules plus S7's conditional field — machinery the spine does not build — landed as a binding **carry-forward section (CF1–CF5: custody-before-certify M6, certify read-source M10b, certify-locally/publish-async M17, review-record calibration fields M11, published-asset-version S7)** with a MUST-import obligation on the weeks 3–8 spec, deliberately kept out of the EARS criteria so the week-3 gate carries no permanently-unverifiable clauses; two spine-real schema criteria back them (per-run outcome fields sufficient for K/K derivation; the outbox schema supporting async-projection reuse). Riders: `UNPINNABLE_PHASE1` reserved in the committed pinning-marker enum now (M12 — one enum constant today versus a schema bump mid-Phase-1) and the workbook break-risk paragraph aligned to the M16 week-0 corpus request (10–20 across feeding teams, up from 2–3). Risk report's gate status annotated with the fold-in.

- 2026-07-27 — **P3 phase-3 mitigation complete on the rest of the register (S1–S11 sweep + M13–M21); all 33 P3 entries now carry a human ruling, cells unchanged at 9/6/9/6/6/9/6 (sum 51).** The human ruled the remaining nineteen 6s and one 4 be split into a **covered sweep** — eleven entries already served by an accepted M-ruling or evidence fact, ruled as one enumerated batch — and nine uncovered entries run one-by-one, continuing the M-series. Sweep outcomes: three held at 6 riding open probes (the P1→P2 cutover seam on the gateway version contract, gateway rate limits on the M4 inquiry, repository-state pinnability on the M7 Octane ask), the Perfecto-outage entry accepted as residual 6 (the ADR 0007 queue seam *was* the mitigation), the unattributable-records entry folded into M8 as a facet, and six re-scored to 4 on existing rules and evidence (contract tests via M10, E4's internal providers, E2/E3's exposure cuts, the Octane schema-drift 4 accepted as-is). The nine individual rulings all accepted as proposed: **M13** the ADR 0010 weeks-0–3 window weakness accepted as residual 4 — the compensating control being that the M-series already converted the review's likely findings into blocking spec-gate criteria; **M14** vendor exit re-scored 4 — M6+M9 custody means no certification history is stranded vendor-side, the remainder is enterprise vendor strategy; **M15** hash-at-ingest adopted (M9's twin; closed by rule at 2) with a per-format canonicalization spec paragraph that also serves P2's cache-key find; **M16** the week-0 real-workbook corpus request filed, contract-test fixtures to be derived from real dialects; **M17** certify-locally/publish-async adopted for the E5 both-ends Octane exposure (PostgreSQL stays the system of record, Octane publication becomes an async idempotent projection) with the M7 ask extended to availability/maintenance windows; **M18** task-zero CI wiring for F1/F2 plus a no-silent-disable line in the working agreement — complying becomes the cheap path, weakening the visible act; **M19** the corpus-class lineage field (`REAL_INGESTED`/`FIXTURE`) plus a real-input floor on the week-3 gate, blocking a silent fixtures-only green (M12's schema-honesty shape); **M20** dual-fixture contract tests keeping the shared ingestion contract source-neutral under deadline cuts; **M21** queue cap/backoff/dead-letter-to-review-queue on the ADR 0007 seam (closed by rule at 2). Six new spec-routed rules join the five from M1–M12 (eleven total awaiting the spine-spec edit pass), plus S7's conditional published-asset-version verdict field. Remaining: the 24-entry P2 register (its flagged probes largely overlap the M-series; the object-*storage* automatic 9 is still the one unaddressed probe), then passes P1, P4, P5.

- 2026-07-27 — **P3 phase-3 mitigation complete on all thirteen 9s (M1–M12); the External integrations column drops from 57 to 51.** Run one item per human ruling, as the method requires — no auto-accepted mitigations. The pass opened with the stakeholder supplying, and the agent restating for confirmation, three evidence facts: **E1** Perfecto is an incumbent vendor (a contract exists; the "contractual void" becomes "the contract is unread"), **E2** the pipeline validates flows on mock data only (confirmed as "mostly" — a residual accidental-real-data entry added at 3), **E3** the system runs inside the bank's validated internal network, with Perfecto explicitly excepted as external SaaS. Two more facts landed mid-pass in answer to direct questions: **E4** the gateway's model providers are hosted inside the bank — resolving routed input R-in 2 outright and closing the gateway-egress 9 at 2 — and **E5** the object repository is ALM Octane, collapsing the five-lens automatic 9 to a scored 6 (with the noted consequence that Octane now sits at both ends of the pipeline). Outcomes across the thirteen: two closed by evidence (M4, M7), ten re-scored to 4–6 under accepted mitigations, and one — the gateway version-reporting contract, both registers' most-corroborated finding — **held at 9** pending the consolidated gateway inquiry, with a dual-source corroboration fallback pre-priced (feasible only because E4 makes the provider team internally queryable). Five accepted rules route to the spine spec: custody-before-certify (no verdict issues until evidence is pulled, classified, and landed on-prem), hash-at-pull into append-only lineage, the quarantine-unknown classifier rule plus Smart Reporting contract tests, certify deriving K/K from bank-held per-run records rather than vendor aggregates, and the review-queue records capturing labeled-set fields from week 0 so Phase 1 produces the judge-calibration set as a byproduct (owner: the stakeholder, same holder as the security-owner role). ADR 0001's Notes updated with the prompt-parity fact from the P3 arbitration and the Phase-1 pinning posture — real prompt version and input/output hashes from day one, `UNPINNABLE_PHASE1` as a distinct provenance enum for what Copilot structurally cannot supply, and the C4 flip schema-enforced at cutover (M12). Four probes open: the Perfecto MSA/DPA read (now carrying the retention, AI-processing, and checksum questions), the consolidated gateway inquiry, the Octane versioning/single-writer ask, and the Copilot license read. Matrix cells updated with ↓ markers where mitigation moved them (Security & privacy 9→6, Interoperability 9→6); the phase-2 consensus column preserved in the report. Honest scope note: E5 named the object *repository*; P2's automatic 9 on self-operated object *storage* is a different store and remains open, as do P3's nineteen 6s and the P2 register.

- 2026-07-26 — **Pass P2 (Reproducibility) phases 1–2 complete; the first matrix row lands at 9/9/9/6/9.** Four blind lens agents (operations, implementation/DX, compliance/audit, delivery) produced 43 raw risk rows, merged to 9 shared clusters and 15 single-lens finds — a 24-entry register recorded in the risk report's consensus log. Two process facts recorded rather than smoothed over: the human elected to blind-score but unsealed before submitting, converting the fifth seat to arbiter; and the original submissions had to be re-emitted by the resumed lens agents after a context summarization, with the implementation lens disclosing its set as a first emission (its initial turn was interrupted pre-scoring) — blindness held, since no lens ever saw another's scores pre-lock. **The one unanimous 9:** the Orchestrator AI gateway's model-version self-report, the single uncorroborated string feeding every pinning mechanism (ADR 0002 cache key, F6 lineage fields, judge calibration) — sharpened by the delivery lens's find that the blueprint ("the pipeline never knows which model answered") and F6's mandatory field flatly contradict. Four human arbitrations: Copilot pinning impact ruled 3 (a top-3 measure that cannot be filled is a failed measure) ⇒ Conversion 9; the automatic 9 on unchosen object-storage technology upheld ⇒ Evidence & provenance 9; the delivery lens's single-lens K-run-erosion find accepted at 9 (the roadmap pre-authorizes the cut; no fitness function asserts K) ⇒ Validation & certification 9; all fifteen single-lens finds admitted at submitted scores with ENV_INFRA likelihood ruled 3 per the unsure rule ⇒ Edge & access rises to 6. Three cheap phase-3 probes flagged: the gateway's per-call version contract, the Perfecto SLA/retention lookup (queued as P3), and an arch-decide selection of the object-storage technology. Mitigation (phase 3) pending; P1 and P3–P5 still queued.

- 2026-07-27 — **Pass P3 (External integrations) phases 1–2 complete; the first full matrix column lands at 9/9/9/6/6/9/9, sum 57.** Five blind lens agents this time — the security & data lens joined P2's four, since the column carries three of the five routed inputs and its headline criterion is Security & privacy. 76 raw risk rows merged to 18 shared clusters and 14 single-lens finds, a 33-entry register recorded in the risk report's consensus log (the reveal canvas said thirteen singles; the enumeration corrected it to fourteen rather than smoothing it over). Process fact recorded: lens completion summaries surfaced in chat before the human submitted a sheet, so the offered fifth-scorer seat converted to arbiter again; blindness held between lenses. **Three findings drew a 9 from every lens that raised them:** the Perfecto contractual void (no DPA, retention, deletion, SLA, or region on record for a SaaS vendor producing and holding a bank's PII-bearing evidence — five lenses scored facets of one missing document set), the gateway version-reporting contract (P2's unanimous find recurring under verifiability, auditability, and evolvability), and the object repository's UNKNOWN capability (five independent automatic 9s). Four cells closed unanimous at 9 without arbitration (security & privacy, verifiability, auditability, interoperability). Four human arbitrations: **the evolvability automatic 9s overridden to 6 by new domain knowledge** — the arbiter supplied a fact absent from the artifact set, that Phase 1's agentic loop is manually invoked in Copilot chat using the very prompts the Phase 2 pipeline will use, so the seam is exercised with production prompt assets from day one (the prompt-parity fact routes to ADR 0001's notes in phase 3); a sustained Perfecto outage ruled impact 2 (the ADR 0007 queue seam converts it to delay, not failure) ⇒ Reliability 6; both Perfecto verdict-integrity facets (error-model drift, unattested K/K) ruled impact 3 for the register — a certification claim resting on unattested vendor reporting is a failed measure; all fourteen single-lens finds admitted at submitted scores, including implementation's perfecto:ai fourth-egress 9 (vendor-side AI processing of screen content with no F3 call site by design) and delivery's judge-calibration prerequisite chain 9 (no owner, no forcing function in the LLM-free spine). Four cheap phase-3 probes flagged: the Perfecto MSA/DPA read, the consolidated gateway inquiry, naming the object repository product and owner, and the enterprise Copilot license's data-processing terms. Mitigation pending for both registers — they overlap enough that a merged pass would price each probe once.

- 2026-07-26 — **Working diagram set assembled** at `components/mobile-test-automation/diagram-set.md`, following the silicon-sandwiches stage-6 template but honestly labeled as stage-6 step 1 done early: stages 1–4 are closed and all ten ADRs Accepted, but risk storming is in flight (pass P2 running at assembly time) and no implementation exists, so this is a working set with named redraw triggers, not a ratification. Three C4-flavored views with the representational-consistency zoom chain: Context (four actor classes, four external dependencies, the P1 finding that conversion reasoning lives in the QA engineer's IDE rather than the system), Container (on-prem deployment boundary from the residency answer, quantum 1-of-1 holding the monolith plus PostgreSQL as its primary store, object storage and Git on-prem outside the quantum, the DB-backed queues drawn as store schemas per the outbox pattern, every external edge tagged `SLA: UNKNOWN (pending)` with a per-edge probe in the SLA register), and Component (all 16 components in their three module subgraphs, the two async seams A1/A2, the thick entanglement edge CV→IM per ADR 0004, the screening library drawn as a hexagon because ADR 0009 says it is not a component, and the CA = 13 provenance fan-in drawn as thirteen undirected edges with a key-level label — a recorded exception to guideline check 4). Two shape corrections vs the stage-2 diagram recorded (externals now double-bordered `EXT:`; screening call edges now solid, since the calls are synchronous and dotted would corrupt the async channel). Six-check self-audit passes with the two recorded exceptions; the misinterpretation test carries ten residual risks with probes, three of which route to the open stage-5 passes. No SVG renders commissioned — the mermaid is the only source of truth until the model stops moving.

- 2026-07-26 — **Stage 5 (arch-risk) opened; assessment frame set.** The spine spec (`docs/sdd/specs/mobile-test-automation-spine.spec.md`) was signed off at the same gate, and the owner elected to run arch-risk before deriving the plan + tasks — reversing the earlier deferral, so the task list *can* now contain mitigation work. Frame recorded in `risk/mobile-test-automation/risk-report.md`: a container-level input diagram extending the stage-3 quantum map with the ADR 0006/0007 data topology and ADR 0008 edge topology, with the honest annotation that **no external dependency has a published SLA on record** (likelihood defaults to 3 across the external column until SLAs are produced — the lookup itself is a candidate mitigation). Criteria = the seven driving characteristics; contexts = the three module clusters plus edge/access and the external integration surface. Five named risk inputs carried in from prior gates rather than rediscovered: the Perfecto device-cloud residency exposure, model-provider egress through the gateway, the ADR 0010 security control that is neither independent nor blocking, the load-bearing (not confirmatory) F1–F3 fitness functions, and self-operated on-prem object storage. Pass queue drafted (security & privacy first, then reproducibility, external integrations, verifiability, remainder); each pass is one criterion or context, blind-scored by 3–5 lenses framed as dimensional coverage rather than independent voters, median-merged, human-arbitrated. **Gate open — awaiting the human's pick of the first storming dimension.**

- 2026-07-26 — **Residency input arrived: on-premises. ADR 0006 Accepted, and stage 4 closes with all ten ADRs Accepted and no open blockers.** The answer resolves the last item in the direction that costs the architecture nothing: on-premises *permits* co-location of the evidence store with the runtime, so the recorded fallback ("co-location illegal rather than merely untidy") stays unpulled, the cluster-C revisit trigger does not fire, and **stage-3 Determination 1 (one quantum) stands unchanged**. The fallback is retained rather than deleted, since a later regulatory ruling isolating evidence would still fire it. Two honest caveats recorded rather than smoothed over. First, **on-premises makes object storage self-operated** — MinIO/Ceph/appliance with its own capacity planning, durability config, backup regime and on-call — which does not change the topology (artifact payloads still do not belong in the transactional store) but does weaken the ADR's "commodity blob storage" cost argument. Second, and more consequentially, **an answer about where data is stored does not settle where it is generated or transits**, which exposes two adjacent residency questions ADR 0006 does not own: (a) **the Perfecto device cloud** — video, page source, screenshots and network captures, precisely the PII-bearing set this ADR classifies, are *produced* in a third-party device cloud and pulled back on-premises, so either Perfecto runs in a private configuration, or an explicit exception covers device execution, or those artifacts must be treated as having already left the premises before the storage rules apply; and (b) **model-provider egress** — prompt payloads leave through the Orchestrator AI gateway, so if that gateway proxies to an externally hosted provider, test content crosses the boundary regardless of where the stores sit, with ADR 0009's egress screening being the control that exists rather than necessarily the control residency requires. Both routed to **stage 5 (arch-risk)** as inputs, not treated as defects. **Sequencing decision at the same gate:** run `sdd-spec` next and defer stage 5 and 6 until before implementation — recorded with its cost, that a task list derived without a scored risk register will not contain mitigation work.

- 2026-07-26 — Post-review gate items **closed one by one; stage 4 now fully settled at ten ADRs**. (1) **ADR 0009 Accepted outright** rather than held pending security sign-off — the gate's explicit choice was to accept on the architecture merits and route the security review as parallel work. (2) The **ADR 0008 security inconsistency resolved by amending the policy rather than the practice**. The stage-4 review had found three ADRs triggering the workspace security threshold (0006 PII/retention/trust boundaries, 0008 identity/auth/authz plus the auditor trust boundary, 0009 PII/secrets/three trust boundaries) resolved three different ways. The root cause was structural, not careless: the criteria require "the designated security owner's approval" and **no separate owner had been designated**, making the clause unfillable and therefore routinely bypassed. Closed by **ADR 0010**, a *target-scoped* override leaving `.arch/adrs/common/approval-criteria.md` untouched for every other target: an ADR triggering a security threshold may be Accepted at the architecture gate, with the review queued as parallel work whose output is a revision or a superseding ADR, never a retroactive status hold. The gate also confirmed the **security-owner role is held by the architecture owner for Phase 1** — recorded as dual-hatted rather than vacant, since a named non-independent reviewer produces findings and a vacant role produces nothing. **The cost is stated plainly in ADR 0010 rather than framed as a process improvement:** an approval control draws its force from independence, blocking authority, and timeliness; this removes the second and the dual-hat removes the first, leaving only timeliness and visibility to protect a top-3 characteristic. Named as a **stage-5 arch-risk input to be scored, not as settled**, with three flip conditions (a separate owner designated, production PII, or a parallel review producing a finding that would have blocked). The enforceable residue is the queue itself — a queue entry at every triggering acceptance and a full drain before first production release — because without it the distinction between deferred and skipped is rhetorical. (3) **ADR 0006 reduced to a single blocker**: the security half is discharged, leaving the residency input, so its Proposed status now means waiting on an input rather than on a signature.

- 2026-07-26 — Stage 4 post-gate critical review **adjudicated**: six findings assessed, four accepted (two of them downgraded from their original framing), two rejected, and two further defects found during the assessment itself. **The one real defect: fitness function F3 was orphaned** — the stage-3 handoff named it load-bearing while listing no ADR to own the decision it enforces, so the stage-2 screening demotion (Screen Untrusted Content, component → shared library at three trust boundaries) had gone unrecorded. Closed by **ADR 0009**, filed Proposed, which owns F3 across all three call sites, splits the assertion into its static and runtime halves (a dependency edge proves availability, not invocation), and states the forfeited structural boundary plainly. The handoff defect is left visible in `style-decision.md` §7 with the general rule it implies: every fitness function must name an owning ADR before a handoff closes. Also applied: an F1–F7 → ADR ownership map, with ADR 0002 named F6's primary owner and ADRs 0004/0006 reduced to referencing it; a provenance line in ADR 0004's Context matching the phrasing ADR 0003 already used for the same situation; a correction of the stale "two named expiry conditions" in the stage-3 handoff, which the post-gate adjudication had reduced to one without sweeping the table; and a clarification that F1–F7 are the cross-cutting subset while the nine ADRs add roughly fourteen more governance mechanisms in their own Compliance sections, which arch-validate must inventory in full. **Rejected with reasons:** the claim that ADR 0002's title undersells the cache key (the Decision states all five fields normatively and Compliance counts them, so the shorthand misleads no one) and the claim that ADR 0005's condensed matrix breaks self-containment (the template explicitly permits concision with deep analysis moved to Alternatives, which is exactly what it does). **Found during the assessment and left for the gate:** ADR 0008 mandates identity, authentication, and authorization plus an auditor trust boundary — the same security threshold that held ADR 0006 at Proposed — yet was Accepted without security-owner approval. Annotated in its Compliance and Notes as outstanding; the status was **not** reverted, because that is a gate decision rather than a correction.

- 2026-07-26 — Stage 4 gate **closed**: seven ADRs Accepted, ADR 0006 held Proposed with its direction confirmed and acceptance blocked on the residency input (plus the security-owner approval its content triggers). One refinement folded into ADR 0001 at the gate: the Invoke Models seam now names its two implementations explicitly — Phase 1 is Copilot chat / agent reasoning behind the same interface, Phase 2 is direct Orchestrator AI calls — with the cutover as a configuration change and nothing else, and a golden-set contract-parity check added to Compliance so the "config change only" promise is testable rather than asserted. Both scope corrections accepted. Preserve Provenance's stage-2 role entry annotated per accepted ADR 0008: it now owns the versioned auditor export (a projection, not a new write path; growth trigger recorded for re-extraction if export demands diverge). **Advance → stage 5 (arch-risk).**

- 2026-07-26 — Stage 4 (arch-decide) **drafted, gate pending**: all eight ADRs from the stage-3 handoff written in Proposed status under `adrs/application/mobile-test-automation/` — none merged, none silently dropped. Each carries the 7-section template with a weighted trade-off matrix, both justifications, and a Compliance section wiring the F1–F7 fitness functions to their owning decisions (F1/F2 → ADR 0001, F4/F6 → ADR 0006, F5 → ADR 0005, F7 → ADR 0004). Two scope labels from the stage-3 handoff corrected with reasons: ADR-6 `common` → `application` (its data topology binds only this system) and ADR-8 `integration` → `application` (it defines this system's own edges, not cross-system communication). ADR 0006 is Proposed with acceptance explicitly blocked on the residency input and additionally requires security-owner approval per the workspace approval criteria (PII, retention, trust boundaries). ADR 0008 assigns the previously unowned auditor export to Preserve Provenance, with the stage-2 role annotation deferred until acceptance. The recency window for decision-context load was empty by fact — these are the target's first ADRs.
- 2026-07-26 — Stage 3 post-gate critical review **adjudicated**: five findings accepted, one rejected, no determination changed, gate remains closed. Accepted corrections, all annotated in place with a new §9 adjudication table in `style-decision.md`: the fidelity-grade entanglement's gateway availability/token cost is a **live coupling paid from day one**, not an "expiry condition" (only the quantum-collapse cost is zero at one quantum); the in-transaction provenance rule now specifies **local transactions with a transactional outbox and idempotent consumers at the async seams, never a distributed transaction across a queue** (carried into ADR-7's scope); the phase-asymmetry claim "cluster A is not in the architecture in Phase 1" was corrected — its CLIs and the review queue are runtime concerns, so cluster A is thinner, not absent; residency's `needs-input` now has a named fallback (evidence isolation fires the cluster-C revisit trigger, making ADR-6 an extraction decision); and "conventions, not structure" was tightened to **build-time governance** — F1/F2/F4 as compile-time dependency rules that fail the CI build — with the contingency warning kept verbatim. Rejected: the implication that naming the enforcement mechanism reduces the cost of declining the microkernel — the protection remains contingent on the rules being built, and that claim stands.
- 2026-07-26 — Stage 3 gate **closed with one variance**. Determinations 1–3 confirmed as recommended. Determination 4 varied: the gate declined the recommended microkernel hybridization and chose a **plain modular monolith**, on the defensible grounds that a plug-in registry is unwarranted machinery for the one source adapter the week-3 roadmap gate needs. The recommendation, the reasons it lost, and the forfeited protection are preserved in the artifact rather than overwritten. Consequence carried rather than absorbed: evolvability's protection moves from structure to enforced convention, so ADR-1 and fitness functions F1–F2 become its entire safeguard, and the Stage 1 worksheet's compensating-control clause was annotated to say so plainly. Counting the stage-2 screening demotion, **three of seven fitness functions now exist to compensate for boundaries removed at human gates** — F1–F3 are flagged release-blocking for arch-validate. Residency remains the one open input, blocking ADR-6 only.
- 2026-07-26 — Stage 3 (arch-style) drafted, gate pending. Four determinations: **one quantum** for both phases (the three divergent clusters are module seams, not quantum boundaries — the IR spine, the CA-13 provenance contract, and the pass-3 entanglement edge all fail the coupling test), a four-concern data topology with lifecycle-partitioned schemas and Git already decided as the asset store, **async only** at the device-replay and human-decision seams with provenance writes deliberately synchronous and in-transaction, and a **modular monolith hybridized with a microkernel** at the source-adapter and model-provider seams. Two deferred ADRs answered in passing: central orchestration over choreography, and the fidelity-grade entanglement accepted synchronously because it costs nothing at one quantum. Also corrected the blueprint's five modules from a technical to a domain partitioning, surfaced the edge topology the component stage excludes (finding: no component owns the auditor export path), and disclosed an anchoring exposure — the methodology chapter read for the readiness criteria also contains the book's worked kata answers, which the skill's seal does not cover.
- 2026-07-26 — Stage 1 and stage 2 gates **closed**. Stage 1 rev 3: Verifiability promoted into the top three over Evolvability on the tiebreaker-asymmetry argument (a characteristic's rank only settles conflicts with lower-ranked opponents, and Evolvability's opponent — reproducibility — is itself top-three), with Evolvability protected instead by a mandatory Stage 4 ADR on the model boundary. Stage 2 rev 3: all three merges directed at the gate applied, nineteen components down to sixteen — metrics as a read model over the provenance store, screening as a shared library at three trust boundaries, and the semantic-fidelity judge folded into Certify Conversion against recommendation. That last merge makes cluster B call the gateway synchronously, so cluster B's "LLM-free" label was corrected to "replay LLM-free", a Verifiability ↔ Reproducibility tension pair was added, coupling was recomputed, the diagram was regenerated, and the entanglement was escalated to Stage 3 and to a Stage 4 ADR rather than absorbed. Four ADRs now carried forward.
- 2026-07-26 — Declared as an OKF bundle: `type` frontmatter added to both Concepts (pure prepend, bodies untouched), recursive catalog generated with `make_bundle.py --recurse-index`, path registered in the OKF binding's `declared_bundles` and `knowledge_globs`, and `index.md` re-grouped by target and workflow stage by hand. Gate green at exit 0 with no new warnings.
- 2026-07-26 — Bundle created: the `mobile-test-automation` stage 1–2 artifacts were decoupled from `.arch/` into this tree per the arch binding's `[roots]` per-target override; the practice katas stay in `.arch/`. Layout mirrors `.arch/` (`<artifact-home>/<target>/`). Convention in [CONVENTIONS.md](../CONVENTIONS.md); linted by `python .cursor/skills/okf-curator/scripts/okf_lint.py`.
- 2026-08-07 — **sdd-roles build item 4 IMPLEMENTED same day (fourth same-day SDD pass): the D6 table-driven emitter — `role-emit` (5th console script) projects the canonical catalog into per-harness layouts; ADR 0003 (catalog-as-source: projections are build outputs, hand-edits are drift) Accepted at the build-gate close.** Clarify C1–C4 owner-locked as recommended (projection facts on descriptor rows at additive `schema_version` 1.3.0; typed-data cards with a declared doctrine-body slot for item 5; golden-projection conformance with no new CHK ids — third exercise of ADR 0002; compose with `write-guard mount` via the extracted shared `mounts.py::render_mount`, byte-certified against the item-3 mount goldens before any feature landed). F1–F4 ratified as drafted: closed `projection` target table ({kind ∈ manifest|agent-card|skill-card|kernel-skill|mount-copy, path_template, format+front_matter map, char_cap as UTF-8 bytes — 30000 on the Copilot `.agent.md` target, null on SKILL.md pending memo §8 probe #1, args_token}); clock-free stamp `sdd-roles <schema_version> catalog:<digest12>`; exits argv→1/render-or-drift→2 with all-or-nothing writes; validator 0.4.0 with restamp via the committed machinery. Corpus: shared 3-role catalog fixture, four golden projection trees (three real harness rows + the deliberately alien `stub-epsilon` row proving the table, not the code, carries the shape), an exit-2 `emitter/failures/` family (cap overflow, unknown body stem, committed drifted tree for `verify`), and a CHK-SCHEMA closedness case. Three decomposition corrections recorded forward in the tasks header (errors/ stays exit-1 per conformance #4; mount-copies carry no stamp — byte-identity with `render_mount` wins; `{args}` substitution rule made mechanical). Gate: selftest 18/18 ×2 byte-identical report files, zero DEFERRED, acceptance PASS on a clean offline copy (1.3.0/0.4.0); tamper trio verified-landed with honest isolation (doctored golden card flips both emitter halves; silenced failure case flips arithmetic alone; un-drifted negative control flips `emitter-verify` alone). Honest notes include one process error caught before any code: a prematurely-written tasks file claiming completion, rewritten to pending — the agent-asserted-completion failure mode the kernel outlaws, caught in the session that builds against it. Port admission #9 added to `kernel/docs/conformance.md`. Next per memo §7: item 5 — the six role doctrine bodies as configs over the kernel, flowing through the emitter's body slot.
- 2026-08-07 — **sdd-roles build item 5 IMPLEMENTED same day (fifth same-day SDD pass): the real catalog — Uncle Bob's six-role conveyor doctrine authored as configs over the kernel at `tooling/sdd-roles/kernel/catalog/`, its projections committed and drift-gated; validator 0.5.0, schema unchanged at 1.3.0.** Clarify C1–C4 owner-locked as recommended (catalog as live kernel data beside descriptors/, goldens in a separate `catalog-projections/` corpus family; bodies as distilled cited doctrine — D2's refutation of verbatim embedding honored, ≈2 KB each with `Sources:` lines; one new `catalog` selftest section with no new CHK ids; NINE bodies — the six conveyor roles plus the three kata-arm composites authored as explicit merges so arms A/B don't render placeholder doctrine, which would confound the pre-registered role-count comparison). F1 froze the 9-role/4-arm registry (specifier/coder/cleaner/architect/hardener/qa + solo/maker3/checker3; arms A, B, C, C-dbg with the ablation marker; gates named unnumbered per D4 — thresholds stay gate config, ch6's CRAP≤6 recorded as workspace configuration, not doctrine; `invocation.prompt` only, carrying role identity per ch5); F2 froze the body layout (H1 · charter · Sources · rules · `## Stage exit` restating gate-results-decide-done); F3 the three real-row golden trees with verify-green-in-place as the standing CI drift command; F4 versions. Every body ends on the anti-BMAD stage-exit rule; backward rework edges (hardener→coder, qa→coder) named in the checker bodies; the hardener's `specs/` scope restricted in-body to ch6's NOOP-example rule. Gate: 19/19 ×2 byte-identical, zero DEFERRED, acceptance PASS (1.3.0/0.5.0); tamper trio each flipping `catalog` red alone — doctrine edit without golden regen (the ADR 0003 scenario live), silenced body (bijection), smuggled harness token (the neutral scan extended over doctrine content). Honest notes include a runbook catch: the tamper compound command hit the Bash time cap mid-tamper, leaving qa.md removed on disk — restored from kept backups with state re-verified, tampers re-run isolated; lesson recorded (one ~3–5-min gate run per command). Items 1–5 of memo §7 now complete; next: item 6, the kata (3 arms + C-dbg × 12 instances × 5 reps, pre-registered §6 design — needs gate implementations bound in kata workspaces and the memo-§8 smoke tests).
- 2026-08-08 — **sdd-roles build item 6 IMPLEMENTED: the kata rig — the deterministic study instrument for the pre-registered §6 A/B/C role-count experiment; ADR 0004 (kata rig as a deterministic instrument, pre-registration pinned by a digest triangle) Accepted; whole-kernel `schema_version` 1.4.0 (first bump since bring-up), validator 0.6.0.** Scoped, at the owner's call, to build the rig and **defer the LLM run**: the machine has only the `claude` CLI (no cursor/copilot) and a full A/B/C×12×5 run is a multi-hour LLM study, so the rig splits into a deterministic half (built, gate-tested) and a non-deterministic half (documented seam `kernel/docs/kata-seam.md`, not built). Built: a 6th console script `kata` with three pure, clock-free subcommands — `plan` (registry + workload + reps → byte-stable `kata_plan`, 240 cells = 4 arms × 12 instances × 5 reps, model resolved at plan time), `analyze` (plan + pre-registration + results → `kata_verdict`; the §6 criteria + kill-rule as **exact integer code**), `report` (verdict → Markdown scorecard, never recomputing). Five new closed schemas (`kata_preregistration`/`workload`/`plan`/`results`/`verdict`; closed set 8→13). The scientific core is a **digest triangle** (ADR 0004): criteria frozen in `kata_analyzer.PREREG_CONSTANTS` → `PREREG_DIGEST` at import → the committed `kata-preregistration.json` constants block (selftest asserts byte-agreement) → every `kata_results.prereg_digest` (analyze refuses on mismatch) — so a post-hoc threshold edit is a loud, atomic amendment, not a silent tweak. Preceded by a **design workflow** (4 scouts → adversarial faithfulness check → synthesis) and a **spec-review workflow** (4 lenses, verdict BLOCK) whose 4 criticals were folded before implementation: C-1 tamper is **run-level** ("fails the run", winner none) not cell-void; C-2 criterion (i) is **two explicit conjuncts** (i-a) ≥A+10pp point margin AND (i-b) strict non-overlap; C-3 Wilson bounds use **ceil-sqrt, both widened** (a floored lower bound narrows the interval and makes a false C-win easier — pinned shut); C-4 the 1.4.0 bump forces a **full-corpus restamp** (8 existing schemas + all valid/invalid/runs/catalog-projections/emitter goldens, via `regen_corpus.py restamp --to 1.4.0` + `goldens` + `role-emit project` regen). C5–C8 defaulted (4 arms/240 cells; `--default-model` `UNBOUND`; z=1.96→9604/2500; CRAP≤6). Owner deferred C1–C4 (the 12 real kata names) to run time; a **placeholder** 12-instance workload pins the pre-registered 12/240 shape now. Gate: `contract-lint selftest` **20/20 ×2 byte-identical**, zero DEFERRED, acceptance PASS on a clean offline copy at 1.4.0/0.6.0; tamper trio each flipping `kata` alone (p-hacking prereg edit caught at `/criteria` by the digest triangle; doctored verdict golden; doctored results fixture). **Honest note — a real bug caught by verification before it reached a golden:** the first integer Wilson formula had a radicand missing a factor of `n`, making intervals ~5× too wide (non-overlap unachievable — a broken instrument); caught by testing the integer math against a float reference across 15 (k,n) cases before cutting any fixture, corrected to match the reference within ≤1 unit while always widening. Also: `mutation_score`/CRAP made integers (float discipline), and `analyze`/`plan` hardened to recompute the prereg digest from the artifact's own constants (a stale-but-matching stored digest had slipped through). Memo §7 items 1–6 now complete — the role-agent kernel is built end to end; the actual kata LLM run remains the deferred seam (needs the cursor/copilot CLIs, the crap4java/mutate4java gate binaries, and the real 12-kata workload).
- 2026-08-10 — **skill-sync drift guard IMPLEMENTED (first full lifecycle test-drive of the usage guide; spec `docs/sdd/specs/skill-sync.spec.md` SPEC-OK same day): manifest-driven byte-diff checker + full-mirror fix + user-scope shadow advisory, single stdlib file, 9/9 fixture selftest, deterministic ×2.** Clarify locked C1=advisory (user scope is not repo-governed; forced by AC-11 + the live `sdd-brainstorm` shadow) and C2=full mirror. First real `check_gate` bound in `.sdd/binding.toml` (was `<none>` since 2026-07-15): guards 83 file-pairs across 5 families (sdd, arch, role-agents, kernel-card, coding-rules-dist); live baseline green with the `SHADOW sdd-brainstorm` advisory. Specifier check-mode audit at spec time caught the ACs pre-resolving both open clarify questions — folded before implementation.
